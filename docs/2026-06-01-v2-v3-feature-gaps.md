# v2 → v3 Feature Gap Audit

Date: 2026-06-01
Branch: `v3-ai`

Tracks every v2 feature missing or reduced in v3. v2 reference code lives in
`src/_old-code/` (also on `main`); v3 code is in `src/`. Data migration is
explicitly **out of scope** here — it is the last step and is tracked separately.

Each item cites the v2 source and the v3 location (or its absence). Verified by
reading code, not inferred.

---

## How to use this doc

- `[ ]` = gap still open, `[x]` = closed (ported / decided won't-do).
- Severity: 🔴 user-facing feature lost · 🟡 capability moved/changed (confirm intent) · 🟢 behavioral delta.
- When closing an item, append the resolving commit or a short note.

---

## 🔴 Functional regressions (user-facing features lost)

- [x] **1. Bulk add notes** — ported (`20afa42b..7e497b0a`).
  - v2: `src/_old-code/components/modals/bulk-add-notes/` (`ConfigureBulkAddNotes.vue`, `BulkProcessNotes.vue`, `bulk-add-note-utilities.ts`) — extract date from title/property, filter by conditions, dry-run preview, per-note connect/merge/override/move/rename.
  - v3: `src/journals/notes/bulk-add/` — `BulkAddService` (`plan`/`apply`), ported `formatToRegexp`, two-stage configure/process modals (filters reuse decoration title/tag/property conditions; per-note skip reasons; all three "ask" decisions; merge; dry-run), `BulkAddFlow`, launched from the per-journal settings page. Reuses `NoteConnectionService`.

- [x] **2. Connect note to a journal** — ported (`8ae32da9..ffb8cb30`).
  - v2: `connect-note` command (`src/_old-code/main.ts` `#configureCommands`) + `ConnectNote.modal.vue`.
  - v3: `connect-note` command (`NoteConnectionCommands`) → `ConnectNoteModal` (journal/date pickers + conditional override/rename/move toggles, or Disconnect when already connected) → `ConnectNoteFlow` → `NoteConnectionService.connect`.

- [x] **3. Disconnect note** — ported (`8ae32da9..ffb8cb30`).
  - v2: `JournalPlugin.disconnectNote` / `Journal.disconnectNote` strips all journal frontmatter keys from a note.
  - v3: `NoteConnectionService.disconnect` strips the journal's frontmatter keys via `FrontmatterService.clearMutator` (with an orphan fallback to the default key set); surfaced through the connect modal's Disconnect button and reused by connect-override and bulk-add. No standalone command (v2 parity).

- [x] **4. Open on startup** — ported.
  - v2: `PluginSettings.openOnStartup` + `openStartupNote()` opened a chosen journal's note in the `onLayoutReady` hook (`src/_old-code/main.ts:425-433`); kept in sync on rename/remove.
  - v3: `src/journals/startup/` — `startupSlice` ({ journalName }), `StartupOpenService` (opens today's note via `OpenJournalEntryFlow` only on genuine launch, gated on `appStartup = !workspace.layoutReady`; reconciles the stored name on journal `renamed`/`deleted`), `StartupBlock` dashboard block. `WorkspaceService` gained `layoutReady`/`onLayoutReady`. Initialized from `main.ts`.

- [x] **5. Delete journal: `clear` / `delete` note handling** — ported.
  - v2: `removeJournal(name, notesProcessing)` with `keep | clear | delete` via `Journal.clearNotes()` / `deleteNotes()` (`src/_old-code/main.ts:233-259`).
  - v3: `NoteConnectionService.disconnectAll` / `deleteAll` (best-effort, snapshot the journal index via `entriesFor`); `DeleteJournalFlow` dispatches on the modal mode and purges before `repository.delete`. `DeleteJournalModal` returns the chosen mode with all options enabled. Delta: `delete` trashes (recoverable) via `NotesService.delete` rather than v2's permanent `vault.delete`.

- [x] **6. Rename journal does not rewrite note frontmatter** — ported.
  - v2: `renameJournal` looped the index and rewrote `FRONTMATTER_NAME_KEY` in every connected note (`src/_old-code/main.ts:224-230`).
  - v3: `NoteConnectionService.reconnectAll(oldName, newName)` (best-effort, snapshots the journal index via `entriesFor`, reuses the shared `#forEachConnected` helper) rewrites only `FRONTMATTER_NAME_KEY` in every connected note — config field names are unchanged by rename, matching v2. `RenameJournalFlow` calls it after a successful `repository.rename`, so a rejected rename (name taken / unknown) leaves notes untouched.

- [x] **7. Calendar sidebar placement (left/right)** — ported.
  - v2: `calendarView.leaf: "left" | "right"` + `placeCalendarView()` using `getLeftLeaf`/`getRightLeaf`, auto-placed on load.
  - v3: per-view `leaf: "left" | "right" | "tab"` field on `viewSchema` (default `right`); `ViewHostService.#leafFor` resolves the leaf via the matching workspace getter (`?? getLeaf(true)` fallback) and `revealLeaf`s it on `#open`; "Open in" selector in `ViewEditSubpage`. Delta: applies on open, not via a live watcher.

- [x] **8. Today highlight in calendar view** — ported.
  - v2: side-panel month grid set `data-today` on the current day (`_old-code` `NotesMonthView.vue:78`).
  - v3: `NotesCalendarCell` emits `data-today` for any cell whose period contains `CalendarDate.today()` (day/week/month/quarter/year). Extension over v2's day-only highlight.

- [x] **9. today/active cell color customization** — ported.
  - v2: `calendarView.todayStyle` + `activeStyle` (each `{ color, background }` `ColorSettings`), edited with color pickers, applied via `v-bind` on `[data-today]`/`[data-selected]`.
  - v3: global `appearance` slice (`src/notes-calendar/appearance/`) with today/active `{ color, background }` (defaults ported from v2), edited via `AppearanceBlock` dashboard block (reuses `UiColorSettingsPicker`); `CalendarAppearanceBridge` applies them as `--journal-cell-*` CSS vars on the active document body; `NotesCalendarCell` consumes them on `[data-active]`/`[data-today]` (today wins). Scoped to calendar cells.

- [x] **10. Week-number column position** — ported.
  - v2: `calendarView.weeks: "none" | "left" | "right"`.
  - v3: per-block `weeks: "none" | "left" | "right"` (default `left`) on the month/week-calendar blocks; `NotesMonthView`/`NotesWeekView` drive visibility + position from it (`data-weeks`), superseding the journal-presence auto-hide (a week number with no week journal now shows as an inactive label, matching v2). The `calendar-timeline` code block gained its own `weeks` field (source-authored) threaded through all four modes, so its week column is author-controllable too.

- [x] **11. Code-block reference help modal** — ported.
  - v2: `CodeBlockReference.modal.vue` (+ `CodeBlockReferenceHint.vue`) — in-settings syntax docs, timeline mode list, home-block options, alias notes, click-to-copy snippets, live previews.
  - v3: `src/journals/settings/ui/` — `CodeBlockReferenceHint` (in the Templates section, between the variable-reference and Templater-support hints, matching v2 placement) opens `codeBlockReferenceModal` → `CodeBlockReferenceModal`. Documents `journal-nav` (+ `calendar-nav`/`interval-nav` aliases), `calendar-timeline` (modes from the shared `timelineModes` + the v3 `weeks` option), and `journals-home` (show/separator/scale/shelf), with click-to-copy `CodeBlockSnippet`s and live previews rendered via `use-code-block-preview-path` (registers a synthetic `JournalsIndex` entry at today's anchor for the open journal, unregistered on close — v2-literal). Delta: registering the synthetic entry at today's anchor temporarily repoints that anchor's index mapping while the modal is open (v2's latent behavior, explicitly chosen).

---

## 🟡 Architectural changes — capability survives in a different shape (confirm intent)

- [x] **12. `change-calendar-shelf` command + global `ui.calendarShelf`** — restored as per-view dynamic commands (decided 2026-07-15). `ViewHostService` registers `journal:change-shelf:<viewId>` ("Change shelf in <view>") per view, gated on shelves existing and the view being open; it opens `shelfPickerSuggest` (All journals + every shelf) and applies the pick to the open leaves via `JournalViewLeaf.setShelf` — the same reactive leaf state the toolbar selector drives.

- [x] **13. `open-calendar` stable command** — restored as a stable alias (decided 2026-07-15). `ViewHostService` registers the fixed `open-calendar` id alongside the per-view commands; it opens the seeded Calendar view, falls back to the first remaining view, and hides only when no views exist. v2 hotkeys bound to `journals:open-calendar` work again.

- [x] **14. `useShelves` toggle** — removed. **Decided won't-do (2026-06-13): always-on shelves is an intentional design decision.**
  - v2: `PluginSettings.useShelves` gated the whole shelves UI; explanatory copy described shelf scoping.
  - v3: no toggle/slice field; shelves always available (`ShelvesDashboardBlock` unconditionally registered). Explanatory copy gone.

- [x] **15. Reload-hint UX (`showReloadHint`)** — restored (decided 2026-07-15) as the session-scoped `ReloadHintService` (a restart inherently clears the flag, so v2's persist-then-reset-on-load reduces to a session ref). `SettingsDashboard` shows a warning banner while a reload is pending; `CalendarWeekBlock` requests it when the apply-globally toggle flips or a preset change touches the active global week patch.

- [x] **16. Dismissable notifications (`dismissedNotifications`)** — gone. **Decided won't-do (2026-07-15): no current content needs the banner channel; reintroduce if a release notice ever calls for it.**
  - v2: `dismissedNotifications[]` + `dismissNotification(id)` + `notifications.ts` registry + dashboard banners with dismiss buttons.
  - v3: no notifications module, slice field, or banner.

- [x] **17. Journal in multiple shelves** — data model reduced to one shelf per journal. **Decided won't-do (2026-07-15): v2's runtime was effectively single-shelf; v3's one-owner model stands.**
  - v2: `JournalSettings.shelves: string[]`.
  - v3: membership only on shelf side (`ShelfConfig.journals[]`); `ShelvesService.assign` removes from all other shelves first. v2 runtime already behaved single-shelf, so likely no real-world loss — confirm and close.

---

## 🟢 Behavioral deltas (intentional-looking; were untested in v2)

- [x] **18. `reset_after` numbering cycle range** — **v3 semantics confirmed (2026-07-15):** cycle spans [anchorValue, anchorValue+count-1]; v2's 0-based modulo was an off-by-anchor bug.
- [x] **19. `allowBefore` uniformity** — **v3 semantics confirmed (2026-07-15):** allowBefore gates all sources uniformly; v2's negative-mirroring branch stays retired.
- [x] **20. Week cross-year anchor** — v3 `WeekPeriod` (`src/calendar/period-week.ts`) fixes the known v2 bug. Improvement, no action.

---

## ✅ Verified fully ported (no action)

- **Decorations** — all 9 condition types + every operator, all 6 style types + options, border modes, shapes/placements, corner placements, icon options, all 3 `ColorSettings` modes, or/and. v3 adds number/boolean property conditions. _Caveats: the `theme` color picker is now free-text (#26); weekday/date condition inputs and border options changed cosmetically (#31–#33)._
- **Write intervals & end conditions** — day/week/month/quarter/year/custom; never/date/repeats; start bound; custom-interval stored-end anchoring + month-end clamping preserved. (v2 `weekdays` write type was dead code — correctly absent.)
- **Numbering** — enabled/anchor/allowBefore/increment/reset_after, generalized to multi-source.
- **Templates** — nameTemplate, dateFormat, folder, templates[], confirmCreation, autoCreate (now midnight-scheduled), all frontmatter fields, all template variables + modifiers, Templater cursor jump.
- **Code blocks** — all 5 names/aliases (`calendar-timeline`, `calendar-nav`/`interval-nav`/`journal-nav`, `journals-home`); all NavBlockRow fields + link variants; timeline week/month/quarter/calendar modes; home options. Edit-row modal preserved.
- **Commands** — open-next/open-prev; all 9 per-journal `type` variants; all 3 contexts; all 4 open modes; ribbon/icon; plugin-level + shelf-scoped commands (unified superset). _Caveat: fresh installs no longer get the 15 seeded default commands — see #21._
- **Calendar week customization** — dow/doy/global locale with presets.
- **Shelves CRUD** — create/rename/remove (with reassign-on-remove), move journal, settings dashboard, create-journal modal, date picker (richer than v2).

---

# Second-pass audit — 2026-06-13

Deeper UI-focused diff of `_old-code/` vs `src/`. Items below were not caught in
the 2026-06-01 pass. `useShelves` (#14) and the markdown-only template suggester
were reviewed and **dismissed** (intentional design / acceptable). Everything
else is tracked here.

## 🔴 Functional regressions (user-facing features lost)

- [x] **21. Fresh installs get zero command-palette commands.** Fixed (`25e4a9d3`): added a `seed` to `commandCollection` with the 15 v2 defaults. Likely an oversight, not a design choice — the `views` collection seeds a default (`src/views/config.ts:53`) but `commands` did not.
  - v2: 15 seeded defaults on fresh install (Open today's/weekly/monthly/quarterly/yearly + next-period ×5 + last-period ×5) — `src/_old-code/defaults.ts:30-205`.
  - v3: `commandCollection = defineCollection("commands", …)` passes no `options.seed` (`src/commands/config.ts:52`; signature `src/settings/schema.ts:31-38`). Migrated users keep theirs via v2→v3 backfill; new users get none.
  - Fix: add a `seed` to `commandCollection` mirroring the 15 v2 defaults.

- [x] **22. Weekday header row (Mon/Tue/…) gone from every in-view calendar.** Fixed (`581b3af5`): both grids now render a weekday header derived from the actual rendered days (stays aligned with the configured first-day-of-week).
  - v2: `<CalendarWeekdays/>` rendered inside both grids — `src/_old-code/components/notes-calendar/NotesMonthView.vue:69`, `NotesWeekView.vue:68`.
  - v3: neither `src/notes-calendar/ui/NotesMonthView.vue` nor `NotesWeekView.vue` renders a weekday header. Affects month-calendar block, week-calendar block, and timeline month/quarter/calendar modes (all reuse `NotesMonthView`). The primitive still exists in the date-picker (`src/calendar/ui/CalendarMonthView.vue:42`) — just not wired into the notes calendar.

- [x] **23. Connect-note date is no longer bounded to the journal timeline.** Fixed (`a3a367e7`): the Connect button is gated on `TimelineService.contains`, with an explanation when the chosen date is out of bounds. Enforced at the modal (where v2 enforced it via the picker); bulk-add's connect path was already timeline-gated in its plan phase.
  - v2: picker bound `:min`/`:max` to journal start/end — `src/_old-code/components/modals/ConnectNote.modal.vue:160`.
  - v3: bare `<input type="date">` with no bounds (`src/journals/notes/ui/ConnectNoteModal.vue:128`); `anchor` resolves via `cycle.anchorOf` with no `timeline.contains` check, and `buildMetadata` doesn't validate bounds (`src/journals/notes/frontmatter.ts:61-75`). A note can be connected to a date outside the journal's span, then won't surface in bounded views.

- [x] **24. Custom-intervals block lost the active-note highlight.** Fixed (`d2d8a9af`): the entry whose (journal, anchor) matches `ActiveEntryViewModel` gets `data-active` and the shared `--journal-cell-active-*` highlight, mirroring `NotesCalendarCell`.
  - v2: the interval row matching the open note got an `is-active` class using the configured active color/background — `src/_old-code/calendar-view/CalendarViewCustomInterval.vue:36,51-57`.
  - v3: `src/views/blocks/custom-intervals/ui/CustomIntervalsBlock.vue` has no active-entry highlighting. (Month-calendar cells still highlight; only this section regressed.)

## 🟡 Architectural changes — capability survives in a different shape (confirm intent)

- [x] **25. Toolbar button action-mode + period-button levels not editable after creation.** Fixed (`abee0316`). Root cause turned out to be deeper than the original note: every toolbar item (and block) defines a `configComponent`, and `ViewsService.updateToolbarItemConfig` already existed and was unit-tested, but **no settings UI ever mounted a `configComponent` or called the update** — `ToolbarItemsList`/`BlocksList` only had add/move/remove. So _no_ post-creation editing was reachable, not just mode/levels. The fix wires the missing affordance: a new generic `EditToolbarItemModal` mounts an item's `configComponent` (working copy + Save/Cancel), and `ToolbarItemsList` gained a per-row edit pencil (shown only when the definition has a `configComponent`) that opens it and persists via `updateToolbarItemConfig`. Separately, `ButtonItemConfig.vue` now exposes the behavior dropdown (select-only/navigate/create) and a toggle per period level (day…year, minimum one, kept in canonical order) for `pick-date`/`current` actions; `navigate-step` buttons show neither (they have no mode/levels).
  - v2: persisted `todayMode`/`pickMode` settings (`navigate`/`create`/`switch_date`) — `src/_old-code/types/settings.types.ts:36-37`.
  - v3: the `mode` field exists in the schema (`src/views/toolbar-items/button/button-config.ts:8`) but its config editor was unreachable, so mode/levels were frozen at the add-time preset.

- [x] **26. Theme color picker downgraded to free-text.** Fixed (`5933d96d`). Restored the named-theme-color dropdown + live swatch (`THEME_COLOR_NAMES` + `UiColorSettingsPicker`), reversing the earlier free-text decision per explicit user request. The dropdown lists the ~32 Obsidian theme variables by name; a previously stored variable that isn't a known theme color stays selectable so existing data round-trips.
  - v2: dropdown of ~32 named Obsidian theme colors + live swatch — `src/_old-code/components/ColorPicker.vue:66-71`.
  - v3: single free-text CSS-variable input, no dropdown/swatch — `src/ui/UiColorSettingsPicker.vue:44`. Affects every color control; typos silently yield `var(--typo)`.

## 🟢 Behavioral / cosmetic deltas

- [x] **27. open-next/open-prev lost feedback + editor gating.** Fixed (`124e8166`). The commands stay available whenever a note is active (editor gating, mirroring v2's `editorCallback`) and `execute` shows a `Notice` for each no-op — "not connected to a journal" / "no next/previous note" — instead of silently disabling. v2 ref: `src/_old-code/main.ts:555-601`.
- [x] **28. Bulk-add: date-format live preview dropped.** Fixed (`124e8166`). The existing `DateFormatPreview` now renders live under the date-format field in `ConfigureBulkAddModal`.
- [~] **29. Bulk-add: dry-run default flipped `false`→`true`.** Kept as the intentional v3 default (user confirmed). Preview-first is the safer default; not reverted. `src/journals/notes/bulk-add/config.ts:47`.
- [x] **30. Bulk-add: live per-note progress indicator removed.** Fixed (`124e8166`). `BulkAddService.apply` takes an `onProgress(done, total)` callback fired after each note; `ProcessBulkAddModal` shows "Processing X of N…" while the batch runs.
- [x] **31. Decoration weekday-condition labels changed.** Fixed (`52c4d782`). Short weekday names ordered from the locale's first-day-of-week, carrying each weekday's true Sunday-first index, via a new `Calendar.weekdaysShort()`. Stored indexes unaffected.
- [x] **32. Decoration date-condition month is now a number input.** Fixed (`52c4d782`). Restored the localized month-name dropdown (`Calendar.months()`) in place of the bare 1–12 number input.
- [x] **33. Decoration border `groove` option dropped (`double` added).** Fixed (`52c4d782`). Re-added the `groove` option (kept v3's added `double`); `style` is `v.string()` so no schema change was needed.
- [x] **34. Icon suggestions no longer alphabetically sorted.** Fixed (`52c4d782`). `UiIconSuggest` sorts its filtered results again.
- [x] **35. `UiCollapsibleBlock` dropped `defaultExpanded`.** Verified — no change needed. Audited every call site: the sections v2 opened-by-default (journals/shelves/commands/views dashboards) all initialize their own `expanded` ref to `true` in v3, a superset of v2's "open when non-empty". The dropped prop lost no capability.
- [x] **36. Folder suggester now includes vault root `/`.** Fixed (`52c4d782`). `FolderInput` drops the vault root (`""`/`"/"`) from candidates, matching v2; `listFolders` is unchanged.
- [x] **37. Timeline quarter/calendar connector lines dropped (cosmetic).** Fixed (`5933d96d`). Re-added the faint grid divider-line CSS to `TimelineQuarter`/`TimelineCalendar`.
- [~] **38. Home-block click always opens all journals of an entry.** Kept as the intentional v3 behavior (user confirmed). Opening all journals of an entry is the deliberate simplification; not reverted. `src/code-blocks/home/ui/HomeCodeBlock.vue:62-68`.

## 🐛 Not a regression — new v3 feature that ships non-functional

- [x] **39. `hideWeekends` toggle is dead → reworked into a per-weekday hide picker.** Was: both calendar blocks emitted a `data-hide-weekends` attribute and the config exposed a single toggle, but nothing consumed the attribute, so it did nothing. Reworked (user-requested): the boolean `hideWeekends` is replaced by `hiddenWeekdays: number[]` (Sunday-based moment day indices, 0–6, range-validated) on both `month-calendar` and `week-calendar` schemas; the config UI swaps the lone toggle for a per-weekday checkbox row ordered by first-day-of-week via `Calendar.weekdaysShort()` (mirrors `ConditionWeekday`). The dead attribute is gone — hiding is now real: `NotesMonthView`/`NotesWeekView` filter out the hidden weekdays' day cells _and_ their header labels, and the month grid's column count is driven by a `--day-columns` CSS var so the remaining columns stay aligned. Schema field is `v.optional(..., [])`, so already-stored configs that still carry `hideWeekends` parse cleanly to an empty hidden set (the dead flag's value is dropped — invisible, since it never did anything). Migration (`v3-to-v4`) and `default-view` write `hiddenWeekdays: []`; the e2e-views fixture updated to match. The same option was extended to the `calendar-timeline` code block (user-requested): an optional `hiddenWeekdays: number[]` (0–6, range-validated) field on `timelineBlockSchema`, authored in the fence YAML (e.g. `hiddenWeekdays: [0, 6]`) and threaded through all four modes (week/month/quarter/calendar) into the same `NotesMonthView`/`NotesWeekView` filter; documented in the code-block reference modal. Covered by unit tests at the schema, config-UI, view-render, and timeline-threading layers, plus two e2e specs — the calendar-block config picker (`settings.e2e.ts`: check Saturday → `hiddenWeekdays` persists `[6]`) and the timeline fence (`code-blocks.e2e.ts`: `hiddenWeekdays: [0, 6]` → month grid drops to five weekday columns). Full e2e suite green.
- [x] **40. View _block_ configs are still not editable after creation.** Fixed. Surfaced while fixing #25: `BlocksList.vue` had the same missing affordance the toolbar list did — add/move/remove only, no edit — so the `configComponent`s of `month-calendar`/`week-calendar` (incl. the #39 `hideWeekends` toggle), `custom-intervals` (journal filter, hide-empty) and `markdown-template` (template path) were unreachable; a block was frozen at its add-time `defaultConfig`. The #25 modal was generalized (`EditToolbarItemModal` → `EditConfigModal`, since it's config-agnostic) and `BlocksList` gained the mirror affordance: a per-row edit pencil (shown only when the definition has a `configComponent`) opening `editBlockModal` and persisting via the already-tested `ViewsService.updateBlockConfig`. Not a v2 regression (the v3 views system is new), but wired up so the views config UI is complete.

---

# Third-pass audit — 2026-07-15

Deep behavioral diff (7 parallel domain sweeps: lifecycle/integration, journals
pipeline, index, calendar view UI, code blocks, settings UI, commands/URIs,
utils/migrations). Focus on interaction details, side-effectful operations, and
parsing tolerance — the layers the first two passes' feature checklists missed.
Items marked **[verified]** were confirmed by reading the cited v3 code directly;
the rest cite exact lines from the sweep but haven't been independently re-read.

## 🔴 Functional regressions (user-facing features lost)

- [x] **41. Opening an entry ignores the indexed note's real path → duplicate notes.** [verified] Fixed: `NoteCreationService.ensureNote` now consults `JournalsIndex.entryByAnchor` before the config-derived path — an indexed entry whose file still exists is reused (frontmatter refreshed, `created: false`); a stale index entry (file gone) falls through to creation at the derived path. Unit tests in `note-creation.test.ts` (reuse, no-duplicate, stale-index fallback; all watched red first) + e2e in `uri-open.e2e.ts` ("relocated entry" — red without the fix, green with it).
  - v2: `Journal.get()` returned indexed metadata carrying the real `path`; `getNotePath()` short-circuited on it (`src/_old-code/journals/journal.ts:126-133,538-544`), so an existing note was re-opened wherever it physically lives.
  - v3: `OpenJournalEntryFlow` → `FrontmatterService.buildMetadata` consults the index only for `endDate` (`src/journals/frontmatter.ts:74-75`), and `NoteCreationService.ensureNote` derives the path purely from the current `nameTemplate`/`folder` and checks only that exact path (`src/journals/notes/note-creation.ts:54-61`). `entryByAnchor`'s stored path is never used in the open pipeline.
  - Impact: any connected note whose real path differs from the config-derived one — connected via connect-note without move/rename, manually renamed/moved, or predating a `nameTemplate`/`folder`/`dateFormat` change — is not found on open; a **second note is created for the same (journal, anchor)**. Likely the biggest source of "v3 keeps missing my notes" reports.

- [x] **42. Pick-date button (default view): no recenter, silent no-op without a note.** [verified] Fixed: `applyMode` in `ButtonItem.vue` now calls `context.setRefDate(anchor)` unconditionally (v2 parity — the interaction always moves the displayed period; mode only decides whether an open follows), so a navigate-mode pick of a note-less date recenters instead of no-opping. Component tests cover recenter in navigate/create for both pick-date and current actions (watched red first).
  - v2: `pickDate` always recentered the displayed month (`refDateMoment.value = …`, `src/_old-code/calendar-view/CalendarView.vue:97-115`), then opened/created.
  - v3: `applyMode` calls `context.setRefDate` only in `select-only` mode; `navigate`/`create` rely on follow-active-date (`src/views/toolbar-items/button/ui/ButtonItem.vue:54-70`). The default view ships pick-date as `navigate` (`src/views/default-view.ts:46`), and `OpenDateFlow` with `existingOnly` errs when no note exists — so picking a note-less date does nothing visible. No v3 mode reproduces v2's "always move display AND open if exists".

- [x] **43. Frontmatter start/end-date toggles no longer rewrite existing notes.** [verified] Fixed: `NoteConnectionService.reapplyAll(journalName)` re-applies the frontmatter write mutator to every connected note; `useReapplyFrontmatterOnToggle` (wired in `FrontmatterSection.vue`, mirroring the auto-create-on-enable composable) fires it when either toggle changes (journal-switch guarded). A stored end equal to the new `CycleService.defaultEndOf` (duration-derived, ignores stored extensions) is treated as period metadata and cleared when the toggle goes off; genuine extensions survive — deliberate improvement over v2, whose "keep divergent ends" branch was dead code (its custom `resolveEndDate` returned the stored end, so toggle-off always deleted, extensions included). Unit-tested at service+composable level (watched red first); e2e in `settings.e2e.ts` ("frontmatter toggle rewrite", red without the fix).
  - v2: `toggleFrontmatterStartDate/EndDate` looped every connected note and added/removed the property (`src/_old-code/journals/journal.ts:444-483`).
  - v3: `FrontmatterSection.vue:52,64` binds the toggles straight to config; the fields apply only at creation/connect time (`src/journals/frontmatter.ts:116-132`). No batch rewrite, no warning hint (unlike the field-rename modal, which does hint — see #57).
  - Impact: toggling silently affects future notes only; existing notes keep stale properties or never gain them.

- [x] **44. Custom journals' `offset` decorations no longer paint the day grid.** [verified] Fixed: `useCellDecorations` gained a binding filter; the month/week grids now gather `scope.all` with custom journals restricted to offset-bearing decorations (`hasOffsetCondition`, exported from `@/decorations`), and `CustomIntervalsBlock` applies the inverse filter so offset decorations stay off interval rows — v2's exact two-surface split. The engine already evaluated day-periods for custom journals (`periodMatchesWrite`, `checkOffset` via `cycle.offsets`); only gathering was missing. Component tests pin both directions plus the existing anti-leak guard (watched red first).
  - v2: the day scope explicitly included custom journals' decorations that carry an `offset` condition (`src/_old-code/composables/use-shelf.ts:28`), so "N days into the interval" markers rendered on month-calendar day cells.
  - v3: `NotesMonthView`/`NotesWeekView` gather decorations from `scope.fixed`, which excludes custom journals entirely (`src/notes-calendar/ui/NotesMonthView.vue:82-84`, `src/notes-calendar/use-shelf-scope.ts:37-39`); custom decorations reach only `CustomIntervalsBlock`. The `offset` condition still exists (custom-only, `src/decorations/settings/ui/condition-types.ts:13`) but can no longer mark day cells. (Note: the third-pass sweep initially reported this inverted — as a leak onto day cells — re-verification shows the opposite: exclusion.)

- [x] **45. Right-click menus lost the guaranteed "Delete" item.** Fixed: `WorkspaceService.openFileMenu` appends a trash-icon Delete item after triggering `file-menu`, wired to the undocumented `fileManager.promptForFileDeletion` via a typed optional interface (v2 parity; `openPathsMenu`'s multi-note chooser funnels into the same method). Label from `m.common_action_delete()`, icon from the central `icons.action.delete`. Unit-tested against the menu mock (watched red first).
  - v2: `showContextMenu` appended a trash-icon Delete wired to `fileManager.promptForFileDeletion` after triggering `file-menu` (`src/_old-code/obsidian-manager.ts:46-62`).
  - v3: `WorkspaceService.openFileMenu`/`openPathsMenu` only trigger `file-menu` (`src/infrastructure/host/internal/workspace-service.ts:134-154`); no `promptForFileDeletion` anywhere in v3.
  - Impact: calendar/nav right-click may offer no Delete unless Obsidian core populates one.

## 🟡 Capability changed shape (confirm intent)

- [x] **46. Journal/shelf commands lost their palette name prefix; names forced globally unique.** Fixed: `DynamicCommandRegistry` registers journal commands as `<journal>: <name>` and shelf commands as `Shelf: <shelf>: <name>` (v2 format, via parameterized paraglide messages); `EditCommandFlow` scopes `takenNames` to same-owner commands (`sameCommandOwner` in commands/config.ts — journal by name, shelf by name, plugin-level as one namespace regardless of write type), so two journals can again both hold an "Open today's note". The edit modal regained v2's hint that the owner prefix is added automatically. Unit-tested at registry/flow/modal level (watched red first).
  - v2: palette showed `<journal>: <name>` / `Shelf: <shelf>: <name>` (`src/_old-code/obsidian-manager.ts:16`), ids namespaced per owner so same-named commands across journals coexisted.
  - v3: `command-registry.ts:60-68` registers the raw name; `edit-command.flow.ts:26-28` enforces uniqueness across ALL commands regardless of target. Two journals can't both have "Open today's note", and palette entries don't say which journal they act on.

- [x] **47. Command ids changed shape → every v2 hotkey on a journals command silently unbinds.** Fixed via id stability, not hotkeys.json rewriting: the v3→v4 migration now keys every backfilled command by its v2 registration slug (`v2CommandSlug` — lowercase, whitespace→dashes, prefix = journal name / `Shelf: <name>` / `""`), so the registered Obsidian id comes out byte-identical to v2's and existing `hotkeys.json` bindings keep resolving. Collision guard suffixes `-2`, `-3`… (impossible in a healthy v2 config). New v3-created commands keep nanoid ids; fresh installs keep `default-*` seeds. Bonus over v2: ids no longer embed the journal name, so hotkeys now survive journal renames (which v2 broke). Caveat: pre-existing v3-beta vaults migrated once with nanoid ids and won't re-run — release-note it. Unit tests pin all three slug shapes + collision; the legacy-upgrade e2e asserts `journals::open-today's-note` re-registers after a real v1 vault migration (red without the fix).
  - v2: deterministic slug ids (`journals:<journal>:<command-name>`, `src/_old-code/obsidian-manager.ts:15,68-70`); Obsidian persists hotkeys by id.
  - v3: user commands get `nanoid()` ids (`edit-command.flow.ts:33`), seeds use `default-*` keys (`src/commands/config.ts:62-106`). No remap exists. `open-next`/`open-prev`/`connect-note` keep their ids. Adjacent to the out-of-scope data-migration work — decide whether hotkey continuity is in scope.

- [x] **48. `{{note_name}}`/`{{title}}` no longer resolve in folder or template-file paths.** Fixed: `NotePathService.pathFor` renders the filename first and binds its basename as `note_name`/`title` into the folder context (v2 order); `TemplateContentService.renderFor` uses one note-name-carrying context for both the template's path and body. Both variables are documented again in the variable-reference modal for the folder-path and template-path contexts (not name-template — circular there). Unit-tested at path/template/modal level (watched red first). Note: `candidateFor` (path→metadata inversion, a v3-only feature) still can't invert a folder containing `{{note_name}}` — inherent ambiguity, unchanged.
  - v2: one shared context carried `note_name`/`title` into folder + template-path rendering (`src/_old-code/journals/journal.ts:546-603,608`).
  - v3: `NotePathService.contextFor` (used for filename, folder, and template path) omits both; they exist only in `bodyContextFor` (template content) (`src/journals/notes/note-path.ts:115-142`, `src/journals/notes/template-content.ts:32-39`). A folder like `Journal/{{title}}` now renders the literal token. Also dropped from the variable-reference modal even for contexts where they still work (`src/journals/settings/ui/VariableReferenceModal.vue` vs v2 `VariableReference.modal.vue:28-38`).

- [x] **49. Auto-create ignores the journal timeline.** [verified] Fixed: `AutoCreateService.createCurrent` gates on `TimelineService.contains(name, today)` before creating (covers both the midnight tick and the settings enable-toggle path, which funnels through it). Unit tests for ended-timeline and future-start journals (watched red first); e2e in `auto-create.e2e.ts` (out-of-bounds journal seeded before "daily" in the fixture; asserts raw vault-file absence because metadataCache lag made a frontmatter assertion vacuously green — test verified red without the fix).
  - v2: `autoCreate()` went through `get()` → `#checkBounds`, so no note outside start/end/repeats (`src/_old-code/journals/journal.ts:486-491,668-684`).
  - v3: `AutoCreateService.createCurrent` builds today's metadata and calls `ensureNote` with no `TimelineService.contains` check (`src/journals/notes/auto-create.ts:44-55`); same for the enable-toggle path. A journal whose timeline ended still gets a note every midnight, which bounded views then won't show.

- [x] **50. Strict fence validation turned v2's graceful degradation into error boxes.** Fixed: `homeBlockSchema.show` filters unrecognized entries via a transform (typo'd entry drops out, valid ones render); `timelineBlockSchema.mode` parses an unknown value to `undefined` so the journal-derived mode applies. Structurally malformed fences still show the error panel (better feedback than v2's silent full reset). Schema tests watched red first.
  - Home block: v2 filtered invalid `show` entries and kept rendering (`src/_old-code/code-blocks/home/home-processor.ts:41-55`); v3's strict union fails the whole block into a `code-block-error` panel (`src/code-blocks/home/home-config.ts:5-11`, `code-block-service.ts:50-56`).
  - Timeline: v2 fell back to the journal-derived mode on an invalid `mode` (`TimelineCodeBlock.vue:27-31`); v3 `v.picklist` hard-errors (`src/code-blocks/timeline/timeline-config.ts:5,10`).
  - Impact: a typo that used to degrade now blanks the block.

- [x] **51. Month/quarter/year header badges lost right-click + hover preview in the default view.** Fixed: `PeriodButtonsItem` badges gained `@contextmenu` → `WorkspaceService.openPathsMenu` and `@mouseenter` → `previewFirstPath` (modifier-gated in the service), resolving paths via `JournalsIndex.pathsAt` — mirroring the in-grid header cells. Component tests watched red first.
  - v2: header badges were `NotesCalendarButton`s with context menu + hover preview (`src/_old-code/calendar-view/CalendarView.vue:180-187`).
  - v3: the `period-buttons` toolbar item binds only click/middle-click (`src/views/toolbar-items/period-buttons/ui/PeriodButtonsItem.vue:81-94`); full behavior survives only in the in-grid heading, which the default view disables (`default-view.ts:94`).

- [x] **52. Multi-journal date disambiguation: at-cursor menu → centered suggest modal.** Restored (decided 2026-07-15): `OpenDateFlow` takes an optional `pickAt` mouse event; when present, disambiguation runs through `WorkspaceService.pickFromMenu` (a native menu at the pointer, cancellation deferred a task past onHide — same ordering hazard as SuggestModal.onClose); keyboard, command, and URI entry points keep the centered suggest. Calendar cells, nav rows, toolbar buttons, and period badges thread their click events through.

- [x] **53. Index rejects notes whose stored date isn't the canonical anchor.** Confirmed deliberate — the week-canonical-anchor design decision (a note left by a same-named journal of a different write type must not be silently re-interpreted). No change.
  - v2 indexed any valid date literally (`src/_old-code/journals/journals-index.ts:91-117`); v3 `parseEntry` requires `isCanonicalAnchor` and unregisters off-anchor notes (`src/journals/frontmatter.ts:40-43`, `src/journals/vault-subscription.ts:62-111`). Deliberate per the week-canonical-anchor design decision — confirm and close, but note it silently hides hand-edited/imported notes v2 displayed.

## 🟢 Behavioral deltas

- [x] **54. Auto-create bypasses `confirmCreation`** — confirmed intended: a background midnight timer must not pop a modal. No change. (`src/journals/notes/auto-create.ts:51` passes `skipConfirmation: true`; v2 prompted). Probably the intended fix for a background timer — confirm and close.
- [x] **55. Template pick stops at the first _existing_ file even if empty** — fixed: `TemplateContentService.renderFor` skips strictly-empty template content and falls through to the next candidate (v2's truthy check). Test watched red first.
- [x] **56. Active-note tracking moved from `file-open` to `active-leaf-change` only** — fixed: `WorkspaceService` also subscribes to `file-open`, so same-leaf navigation (link click, open-in-place) updates the active-note signal; consumers are idempotent, so the occasional double emit is harmless. Test watched red first. (`src/_old-code/main.ts:515-519` vs `src/infrastructure/host/internal/workspace-service.ts:31-35`) — same-leaf navigation may not update the calendar highlight depending on host event semantics.
- [x] **57. Frontmatter field rename no longer rewrites note properties** — confirmed intended: the modal explicitly hints notes are not rewritten (contrast #43, whose silent variant WAS fixed). No change. — v2 rewrote every note's key (`src/_old-code/journals/journal.ts:427-442`); v3 changes config only and shows a hint (`EditFrontmatterFieldModal.vue:42`). Hinted, so presumed intentional — confirm and close (contrast with the silent #43).
- [x] **58. Hover preview needs the modifier already held at mouseenter** — restored (decided 2026-07-15): the modifier gate moved from `WorkspaceService.previewFirstPath` into the shared `useModifierHoverPreview` composable, which fires on enter-with-modifier AND on Ctrl/Cmd pressed mid-hover (window keydown listener alive only between enter and leave). Wired into calendar cells, nav rows, and period badges.

---

# Fourth-pass audit — 2026-07-15

Six parallel domain sweeps (journals pipeline/index, calendar view UI, code
blocks, config modals/editors, lifecycle/host/commands, utils/migrations),
targeting the sub-checklist layer the first three passes didn't reach: anchor
canonicalization, UI↔domain sync, per-field validation, and parsing tolerance
edges. Items marked **[verified]** were re-read against v3 source directly in
this pass; the rest cite the sweep's lines and still need an independent read.

## 🔴 Functional regressions (user-facing features lost)

- [x] **59. Auto-create & startup-open write a non-canonical `journal-date` for non-daily journals → orphaned/duplicate notes.** Fixed: both sites now resolve today via `CycleService.anchorOf(name, CalendarDate.today())` before invoking the flow / `buildMetadata` (skipping if the journal can't resolve), matching the command-registry/URI callers; `anchorOf` falls back to pure interval math for custom cycles so it's safe at boot before the index fills. Unit tests in `auto-create.test.ts` + `startup-open.test.ts` (monthly journal → `journal-date` = month anchor, watched red first) and an e2e in `auto-create.e2e.ts` (monthly boot note stamped `…-01`, red without the fix). [verified] `StartupOpenService.#open` (`src/journals/startup/startup-open.ts:40`) and `AutoCreateService.createCurrent` (`src/journals/notes/auto-create.ts:47`) both pass `CalendarDate.today().toAnchor()` — today's _exact_ date — straight into `OpenJournalEntryFlow`/`buildMetadata`. `buildMetadata` uses the anchor verbatim and `writeMutator` writes `fm[dateField] = metadata.anchor` (`src/journals/frontmatter.ts:70-84,110`) with no canonicalization. The `anchor` parameter is a canonical-anchor-by-contract: every other caller resolves it first via `cycle.anchorOf` (`src/commands/command-registry.ts:166`, `src/journals/uri/journal-uri-handler.ts`) or `period.anchor` (calendar cells).
  - v2: `autoCreate()`/startup went through `Journal.get(today)` → `resolveForDate`, which canonicalized to the period anchor before building metadata (`src/_old-code/journals/journal.ts:127,486-491`, `src/_old-code/main.ts:425-433`).
  - Impact: for week/month/quarter/year journals, the auto-created/startup note stores a mid-period `journal-date` (e.g. `2026-07-15` for a monthly). On the next vault walk `parseEntry` rejects it as non-canonical (`frontmatter.ts:40-43`) → the note never indexes/connects, and each midnight/startup re-creates or re-mis-writes it. Custom-interval journals additionally get a wrong _filename_ (today's date, not the interval-start anchor). Not caught by tests: `startup-open.test.ts` uses only day journals; the non-daily journal in `auto-create.test.ts` has `autoCreate:false`. Fix: `cycle.anchorOf(name, CalendarDate.today())` at both sites before invoking the flow / `buildMetadata`.

- [x] **60. Sequential-numbering anchor is never taken from the start date (v3-created journals).** Fixed: `NumberingService` now resolves the effective anchor as `config.timeline.start || config.numbering.anchorDate` (a defined start date wins, matching the settings UI's "Start date is used as anchor date" label and v2's start→anchorDate watch) in both `#compute` and `anchorForNumbers`, with the resolved anchor folded into the per-journal cache fingerprint so a start-date edit invalidates. v2-faithful: migrated journals already carry `anchorDate == start`, and start-empty journals still use the picked `anchorDate`. Unit tests in `numbering.test.ts` (start-as-anchor for `assignNumbers` and `anchorForNumbers`, watched red first — was `index: NaN` / `None`); the real inversion path stays green in `auto-attach-index.e2e.ts`. [verified] `SequenceSection.vue:79-86` hides the anchor picker when `timeline.start` is set and shows "Start date is used as anchor date", but nothing writes `timeline.start` into `numbering.anchorDate` — the sole writer is the hidden picker (`SequenceSection.vue:38`). `NumberingService.#compute` reads `numbering.anchorDate` directly (`src/journals/numbering.ts:47,61`), which stays `EMPTY_ANCHOR` (`""`) for fixed journals (`src/journals/config.ts:310,324`).
  - v2: a `watch` on `config.start` unconditionally synced `config.index.anchorDate = start` (`src/_old-code/settings/JournalSettingsEdit.vue:307-315`), so the domain anchor genuinely mirrored the start date.
  - Impact: a journal **created in v3** with a start date + sequential numbering computes indexes from an empty anchor (absent/wrong "Day N") while the UI claims the start is the anchor. Migrated journals are spared — `v3-to-v4` carries the old `index.anchorDate`. Fix: sync `timeline.start → numbering.anchorDate` (or fall back to `timeline.start` in `NumberingService` when `anchorDate` is empty).

## 🟡 Capability changed shape / behavior changed (confirm intent)

- [x] **61. Today vs pick-date create/navigate roles inverted from v2.** Fixed (user chose restore-v2, 2026-07-15): `default-view.ts` seed swapped back to `pick-date` → `mode: "create"` and `current` (Today) → `mode: "navigate"`, so Today recenters + opens today's note only if it exists (never creates) and the picker creates at the picked date. Only fresh installs (and migrated users who never set `todayMode`/`pickMode`, where the seed shows through) were affected — the v3→v4 migration already maps explicit v2 modes onto the seed. `default-view.test.ts` updated to the restored expectations. [verified] v2 defaults were `todayMode: "navigate"` (Today recenters + opens an _existing_ note, never creates) and `pickMode: "create"` (the picker creates) — `src/_old-code/defaults.ts:161-162`. v3's default view ships `pick-date` as `mode: "navigate"` and Today (`current`) as `mode: "create"` (`src/views/default-view.ts:46,51`); `ButtonItem.applyMode` maps `create → existingOnly:false` (`ButtonItem.vue:65-71`). Net: **clicking Today now silently creates today's note** (v2 never created from Today), and the date-picker no longer creates at the picked date. Not recorded in the deliberate-deviations notes. Decide intent; if unintended, swap the two default modes.

- [x] **62. Year paging (`«` / `»`) snaps the calendar to January.** Fixed: `ButtonItem`'s `navigate-step` branch now shifts the ref date itself by the unit (`CalendarDate.shift(±n, {week:"w",month:"m",quarter:"q",year:"y"})`) instead of snapping to the stepped period's anchor, so paging by a unit coarser than the grid keeps the displayed month (v2 parity — navigate always did `refDate.add(±n, unit)`). Month/week paging is visually unchanged (the grid shows the whole period); the day within the period is now preserved. `ButtonItem.test.ts` updated (month/week keep the day) + a new year-paging-keeps-May test (watched red first: `2027-01-01`→`2027-05-15`); `view.e2e.ts` toolbar month-nav round-trip stays green. [verified] The default view wires prev/next-year as `navigate-step` `unit:"year"` (`default-view.ts:62-87`); `ButtonItem.fire` sets `refDate` to the year period's anchor = Jan 1 (`ButtonItem.vue:87-95`), so paging from July 2026 lands on January 2027. v2 did `refDate.add(±1,'year')`, preserving the displayed month (`src/_old-code/calendar-view/CalendarView.vue:80-88`). Month-step paging is unaffected. Fix: shift the ref date by a year keeping month/day rather than snapping to the year anchor.

- [x] **63. Rename-journal modal hint contradicts what rename now does.** Fixed: removed the `journal_notes_not_rewritten_hint` from `RenameJournalModal` (rename rewrites the journal key in every connected note via `reconnectAll` — the #6 port — so "Existing notes are not rewritten" was false; rename just works, so no hint is warranted per minimal-noise). The message key stays — it's still correct in `EditFrontmatterFieldModal`/`EditSequencePropertyModal` (config-only, #57). Removed the now-invalid presence test. [verified] `RenameJournalModal.vue:41` shows `journal_notes_not_rewritten_hint` — "Existing notes are not rewritten. You may need to update them manually." — but `RenameJournalFlow` calls `NoteConnectionService.reconnectAll`, which rewrites the journal key in every connected note (the #6 port). The copy (likely reused from the frontmatter-field-rename modal #57, where it _is_ correct) tells users the opposite of reality. Fix: drop/replace the hint.

- [x] **64. Bulk-add: several v2 affordances dropped.** All three fixed.
  - **64a (property-name required in property mode)** Fixed: `bulkAddParametersSchema` gains a `v.forward(v.partialCheck(...))` requiring a non-blank `propertyName` when `datePlace === "property"` (reusing `journal_property_name_required`), and `ConfigureBulkAddModal` surfaces `errorBag.propertyName` in the field's #description — a blank name now blocks submit with an error instead of silently connecting nothing. Component test watched red first.
  - **64c (date-format prefill)** Fixed: the modal prefills `dateFormat` from the journal's configured format (via `JournalsViewModel.getJournal`), falling back to `YYYY-MM-DD`, matching v2. Component test watched red first.
  - **64b (conflict-decision row identity)** Fixed: `PlannedAction` gained `targetPath` (the configured destination path; `occupant` already existed), and each action row in `ProcessBulkAddModal` now renders a #description showing the occupying note (`bulk_add_occupant`) and the destination path when it differs from the source (`bulk_add_target`) — so users can see which note holds the slot and where a moved/renamed note lands before choosing skip/override/merge and keep/move/rename. Component test watched red first.

- [x] **65a. Property decoration condition: value input not gated on operator.** Fixed: `ConditionProperty.vue` gains a `showValueField` computed (`op !== "exists" && op !== "does-not-exist"`) wrapping the text/number/date value inputs, so an existence check no longer shows a meaningless operand field (v2 parity). Component tests for text/`exists` and number/`does-not-exist` (watched red first); the date-derivation test now selects a comparison op to reveal the input. **65b** (the sibling silent-no-save issue) is fixed separately below.
- [x] **65b. Property decoration condition: empty name is schema-invalid but Save stays enabled and silently no-ops.** Fixed: the property-`name` `minLength(1)` in all four property-condition schemas now carries the `journal_property_name_required` message, and `ConditionProperty.vue` renders that field's `errorMessage`. Clicking Save with a blank property name now blocks (as before) but shows the error instead of silently no-opping. Modal-level test (property condition with blank name → Save → error shown, not submitted) watched red first. [verified]

- [x] **66. Custom week-settings modal doesn't prefill from the active locale.** Fixed: `Calendar.localeWeek()` now exposes the locale's `{dow, doy}`, and `WeekPresetPickerModal` seeds its custom fields from the effective week — `current`'s dow/doy in custom mode, else the locale week — instead of a hardcoded Monday/4. Opening Custom from locale mode no longer risks silently overwriting a non-ISO locale default. Component test seeds a Sunday-start locale and asserts the round-tripped submit (watched red first). [verified] `WeekPresetPickerModal.vue:32-33` seeds the custom day fields from `current.dow/doy` only when `mode==="custom"`; in the default `locale` mode it falls back to hardcoded Monday/`4`, so a non-ISO-locale user clicking "Custom" can silently overwrite their real locale default (v2 always seeded from the effective config — `CalendarWeekSettings.modal.vue:42-54`).

## 🟢 Behavioral / cosmetic deltas

- [x] **67. Code-block parsing tolerance narrower than v2 on new axes (extends #50).** Fixed: both `timelineBlockSchema` and `homeBlockSchema` now front the object schema with `v.pipe(v.unknown(), v.transform(asRecord), v.object({...}))`, so a non-mapping fence (`mode:month`/`show:day` with no space → a bare YAML string) degrades to defaults instead of erroring into a panel. Home field values are tolerant: `separator` uses `v.fallback` (null/wrong-type → `" • "`), and `scale` coerces via `v.fallback` + `n => n || 1` (non-number → 1, and `0` → 1 so the block stays visible), matching v2's `|| ` coercion. `HomeCodeBlock.effectiveShelf` treats an empty `shelf` as unset (current shelf), not a literal `""`. Schema + component tests for each axis watched red first. [verified]
- [x] **68. "Today" highlight over-applied to week-number and month/quarter/year header cells.** Confirmed intentional (decided 2026-07-15): part of the #8 highlight extension; the week-number/header-badge highlight stays. No change. (sweep-cited) `NotesCalendarCell` sets `isToday` for any cell whose period contains today (`NotesCalendarCell.vue:23`), so the current week's week-number cell (and header badges when shown) light up; v2 highlighted only the day cell. Adjacent to the intentional highlight-extension (#8) — but the week-number cell lighting reads as a bug.
- [x] **69. Auto-create timing changed: hourly poll → single next-midnight timer, first tick pre-layout-ready.** Confirmed intentional (decided 2026-07-15): the single next-midnight timer stays; a mid-day-deleted current note returns at next midnight rather than within the hour. No change. (sweep-cited) `auto-create.ts:28-30` reschedules for next local midnight (v2 re-polled hourly — `_old-code/main.ts:456-461`), so a mid-day-deleted current note isn't recreated until midnight and missed-midnight recovery depends on `setTimeout` catch-up on wake; and `#tick` runs during `onload`, not gated on `onLayoutReady` (contrast startup-open/migration). Likely intentional — confirm.
- [x] **70. Focusing the calendar sidebar clears the active-day highlight.** Fixed: the `active-leaf-change` handler now skips leaves with no file, so focusing the calendar `ItemView` no longer emits `active-note-changed(None)` and the highlight persists (v2 tracked only `file-open`; note closes leave the last note active, as in v2). `file-open` still drives same-leaf navigation (#56). Unit test replaced the old None-on-fileless assertion with a persist assertion (watched red first). [verified] the `active-leaf-change` handler emits `active-note-changed(None)` for any non-`MarkdownView` leaf (`workspace-service.ts:41-44`), so focusing the calendar `ItemView` drops `ActiveEntryViewModel.active`; v2 tracked only via `file-open`, which doesn't fire on sidebar focus. Side effect of the #56 change; an `instanceof MarkdownView`/null-file guard restores v2.
- [x] **71. Minor edits/defaults.** All items addressed.
  - **`o` ordinal token in path inversion** Fixed: `src/templates/format-regex.ts` now supports `o` (reading the locale's `dayOfMonthOrdinalParse` like the bulk-add copy), so a `Do`-style `dateFormat` re-derives from a filename via path inversion. Test watched red first.
  - **`UiTemplateInput` empty-focus** Fixed: the fetch returns `[]` for an empty query (v2 parity), so focusing an empty template field no longer pops the whole vault's template path list. Test updated red-first.
  - **`{{relative_date}}` ±7 wording** Fixed: `formatDay` tightened the named-weekday window to ±(2-6); exactly a week now reads "7 days ago" / "in 7 days" (v2's `sameElse` bucket), not "Last Wednesday". Tests for both boundaries watched red first.
  - **Decoration "Add style" defaults** Fixed: `defaultStyle` restored to v2 — a new color style defaults to the visible `text-normal` theme color (not transparent), a new border shows its left side, a new shape is a circle at the bottom, and a new icon sits at the top. New `defaults.test.ts` watched red first.
  - **Shelf-name placeholder** Fixed: `ShelfNameModal` regained an example placeholder (`shelf_name_placeholder` = "e.g. Work").

---

# Fifth-pass audit — 2026-07-16

Six parallel domain sweeps (journals pipeline/index, calendar view UI +
decorations, code blocks, views/toolbar system, settings/config UI,
lifecycle/host/commands/migration), re-run against `_old-code/` after four prior
passes. Mandate sharpened to the residue those feature-level passes could still
miss: per-field options, defaults, event/mode gating, parse-tolerance edges,
reactive syncs. Every item below was **independently re-read against v3 source**
by the coordinator (not just the sweep) before landing here; confidence + exposure
noted honestly. Items open (`[ ]`) — no fixes applied, decisions pending.

## 🔴 Functional regressions (user-facing features lost)

- [x] **72. `{{relative_date}}` renders empty for custom-interval journals.** [verified] Fixed: `buildNavRowContext` now resolves the custom case via `customRelativeDate` (`src/code-blocks/nav/nav-row-context.ts`) — it counts intervals from the one containing today (`cycle.anchorOf(today)`) to the row's interval (`cycle.countRepeats`) and renders "This/Last/Next `<journal>`" or "N `<journal>`s ago / from now" via new `relative_date_custom_*` messages, restoring v2's `CustomInterval.resolveRelativeDate` labels (`src/_old-code/journals/custom-interval.ts:92-112`). The existing test that asserted empty-for-custom was replaced with the five label cases (this/last/next/ago/from-now, watched red first), and the `journal_edit_variable_relative_date_description` help text — which previously read "empty for custom journals" (a deliberately-codified simplification, reverted here per v2 fidelity) — now documents the custom labels.
  - v2: `CustomInterval.resolveRelativeDate` returned real labels ("This/Last/Next `<journal>`", "N `<journal>`s ago / from now") — `src/_old-code/journals/custom-interval.ts:92-112` — wired into nav rendering at `NavigationBlockRow.vue:56-58`.
  - v3 (before): `fixedPeriodKindFor` returned `null` for `{ type: "custom" }`, so `relative = ""` bound empty; `relativeDate()` handled only day/week/month/quarter/year.
  - Exposure was opt-in — the custom default nav rows use `{{journal_name}} {{index}}`, not `{{relative_date}}`, so only users who added it were affected (which is why four passes missed it).

## 🟡 Capability changed shape / behavior changed (confirm intent)

- [x] **73. Home fence: `show`-as-scalar and `shelf`-as-non-string hard-error where v2 degraded (new tolerance axes beyond #67).** [verified] Fixed: `homeBlockSchema.show` now wraps its array pipe in `v.fallback(..., ["day"])`, so a scalar (`show: month`) degrades to the default instead of failing validation — mirroring v2's caught-`.filter`-throw fall-through; `shelf` gains a coercion transform (`typeof string → as-is`, `number`/`boolean → String()`, else unset) so an unquoted `shelf: 2024` becomes `"2024"` and simply matches no shelf, reproducing v2's pass-through rather than erroring (`src/code-blocks/home/home-config.ts`). Two schema tests watched red first (scalar-show → `["day"]`; numeric-shelf → `"2024"`); the empty-list-on-all-invalid case still parses to `[]` (fallback fires only on a non-array, not an empty result).
  - v2: `home-processor.ts:41,48` wrapped `config.show.filter(...)` in a try/catch returning defaults on any throw, so `show: day` (scalar, a common "forgot the list" YAML slip) → `"day".filter` throws → caught → default; `shelf` was passed through untouched, so a numeric/unquoted `shelf: 2024` rendered harmlessly.
  - v3 (before): `homeBlockSchema.show` was `v.array(v.unknown())` — the `v.optional` default fires only on `undefined`, not a type mismatch, so a scalar failed array validation; `shelf: v.optional(v.string())` had no `v.fallback` (unlike the deliberately-softened `separator`/`scale` beside it), so a non-string failed too, both propagating to the strict `code-block-error` panel. #67 had softened only the within-array `show` filter, `mode` fallback, non-mapping fence, and separator/scale — these two field-level type mismatches were uncovered axes.

- [x] **74. Journal view leaf never sets `navigation = false`.** [verified] Fixed: `JournalViewLeaf` now sets `navigation = false` (`src/views/view-leaf.ts`), restoring v2's `CalendarView` flag so a `leaf: "tab"` view opts out of navigation history and isn't reused/replaced when a note opens. No unit test: the flag is framework config with no branching logic, and its effect is Obsidian-internal (not observable through the fake host), so a `navigation === false` assertion would only mirror the source line — skipped per the no-trivial-tests / don't-test-the-wiring guidance.
  - v2: `CalendarView` set `navigation = false` on the `ItemView` (`src/_old-code/calendar-view/calendar-view.ts:8`) — Obsidian's documented convention for calendar-type views.
  - v3 (before): `JournalViewLeaf` inherited the `ItemView` default (`true`); v2 was sidebar-only and immune by construction, but v3 allows `leaf: "tab"` placement where a navigable calendar tab is added to navigation history.

- [x] **75. Startup view-open reveals/expands the sidebar and steals focus; v2 only placed the leaf.** [verified] Fixed (user chose place-only, 2026-07-16): `#openStartupViews` now calls a new `#placeOnStartup(id)` that leaves a leaf already restored from the persisted layout untouched and places a genuinely missing one via `setViewState({ type })` — no `active: true`, no `revealLeaf` — so a collapsed sidebar stays collapsed and focus is not stolen (`src/views/view-host.ts`). Explicit open commands (`open()`) still reveal. Tests watched red first: startup places without revealing, and an already-restored leaf is left untouched; a guard pins that the open command still reveals (the fake host gained a `revealLeafCalls` counter).
  - v2: `placeCalendarView()` on `onLayoutReady` did `getRightLeaf(false).setViewState({ type })` only — no `revealLeaf`, no `active: true` (`src/_old-code/main.ts:288-300,384-396`). A user-collapsed sidebar stayed collapsed; reveal happened only via the `open-calendar` command.
  - v3 (before): `#openStartupViews` → `open()`, which found the layout-restored leaf and called `revealLeaf(existing)`, force-expanding the sidebar and stealing focus every launch. Distinct from #4 (startup _note_) and #7/#13.

- [x] **76. Decoration property exclusion operators now treat a missing property as satisfied.** [verified] Fixed (user decision, 2026-07-16) — a _unified_ semantics, going beyond both v2 and prior v3: `checkProperty`'s absent-property branch returns true for the exclusion operators `neq` and `does-not-contain`, and false for every positive operator (`eq`/`contains`/`starts-with`/comparisons) (`src/decorations/engine-checks.ts`). Rationale: an exclusion condition is satisfied when there is no value to violate it — a note lacking the property counts as "not equal to X" and "does not contain X". Unit tests for neq (text/number/date) and does-not-contain on a missing property, plus a guard that a positive operator stays false (watched red first).
  - v2: `case "neq": return properties[name] != condition.value` → absent property `undefined != value` is **true** (decorated), but `does-not-contain` returned **false** for absent (`src/_old-code/composables/use-decorations.ts:181-206`) — inconsistent between the two exclusion operators.
  - Prior v3: `if (!present) return false` for all value operators — absent was false for both `neq` and `does-not-contain`. The new rule makes the two exclusion operators agree (true on absent), unlike either predecessor. Release-note: a `neq`/`does-not-contain` decoration now also paints notes that lack the property entirely.

- [x] **77. Numbering dropped back-propagation from a later existing (manually renumbered) note.** [verified] Fixed (user chose restore-v2, 2026-07-16): `NumberingService.#compute` now falls through nearest **previous** existing note → nearest **next** existing note → config anchor, via a shared `#basisNumbers` helper (`src/journals/numbering.ts`). `countRepeats` is signed, so the next-note basis subtracts the steps between them — matching v2's `after.index - repeats`; `#cascade` already normalizes negative steps for both reset kinds. Unit tests watched red first: back-computes from a later manually-numbered note when no earlier note exists (`100` two weeks earlier → `98`), plus a guard that an earlier note still wins over a later one (v2 ordering).
  - v2: `#resolveIndex()` tried nearest **previous** existing note → nearest **next** existing note (`after.index - repeats`) → config anchor (`src/_old-code/journals/journal.ts:686-737`), so a future note's manually-edited index back-computed earlier dates.
  - v3 (before): `NumberingService.#compute` consulted only `findPrevious` — no `findNext` fallback — then fell to the config anchor, so an _earlier_ period with no earlier neighbor ignored a later manual value. Forward propagation was unaffected.

- [x] **78. Template-file suggester scope narrowed to markdown + configured template folders.** **Decided won't-do (2026-07-16): the narrowing is an intentional UX improvement.** Templates live in the template folder, so scoping the suggestions there (and to markdown notes) yields a focused list instead of a firehose of every vault file; an out-of-folder or non-markdown template is still reachable by typing the path manually.
  - v2: `template-suggest.ts` returned **any** `TFile` vault-wide whose path contains the query (markdown or not, any folder).
  - v3: `UiTemplateInput` → `TemplatesService.candidatePaths` returns markdown notes only, and when a core-Templates/Templater folder is configured, restricts to files inside it (`src/infrastructure/host/internal/templates-service.ts:35-40`).

## 🟢 Behavioral / cosmetic deltas

- [x] **79. Theme-color dropdown lost v2's friendly labels (shows raw variable names).** [verified] Fixed (user chose full i18n, 2026-07-16): ported v2's 32 friendly labels as `ui_theme_color_*` messages and added a `themeColorLabel(name)` helper (`src/ui/theme-colors.ts`) backed by a static map of message getters (statically referenced, so tree-shakeable), falling back to the raw variable name for an unknown stored value; `UiColorSettingsPicker` renders `themeColorLabel(colorName)` for each option. All 32 labels translated to every locale. Component test watched red first (the `text-normal` option shows "Normal text" and carries value `text-normal`).
  - v2 mapped each of ~32 variables to a human label ("Primary background", "Error background, RGB value") via `ui-texts.ts:15-48` → `ColorPicker.vue:66-70`; v3 previously kept bare variable strings, rendered verbatim. Residual of #26 (the dropdown/swatch was restored but not the labels).
- [x] **80. `connect-note` command lost editor-mode gating (parallel to #27, different command).** [verified] Fixed: the command's `check` now gates on `WorkspaceService.hasActiveEditor()` instead of `activeNote().isSome()`, so it appears only with an active markdown editor — not in reading mode or on a non-markdown file (mirrors the #27 open-next/prev fix). Test watched red first (a file active but no editor → unavailable). v2 registered it with `editorCallback`, so it appeared only with an active **markdown editor** (`src/_old-code/main.ts:603-614`); v3 gates on `check: () => this.#workspace.activeNote().isSome()`, which is true for any active file incl. reading mode / PDF / image (`src/journals/notes/note-connection-commands.ts:16-19`). #27 fixed this class for `open-next`/`open-prev` but named only those.
- [x] **81. Sequence "Anchor date" field lost its explanatory help text.** [verified] Fixed: `SequenceSection.vue` now always renders the core sentence via a new `journal_edit_anchor_description` message ("This date is used to connect some number to it for further calculations.", ported from v2 and translated to all locales), and still appends `journal_edit_anchor_start_used` when a start date is set. Component test watched red first (description present with no start date). v2's anchor-date row always read "This date is used to connect some number to it for further calculations. Start date is used as anchor date if defined." (`JournalSettingsEdit.vue:384-393`). v3 renders only `journal_edit_anchor_start_used` ("Start date is used as anchor date.") and **only when a start date exists** (`SequenceSection.vue:80-86`); the no-start-date case — exactly when the picker shows and the user must choose — has no description, and the core sentence has no message key.
- [x] **82. `refDate` persists across restarts; v2 always opened at today.** [verified] Fixed (user chose a per-view setting, 2026-07-16): added a `rememberDate` boolean to `viewSchema` (default **false** = v2's "open at today"), a "Remember last viewed date" toggle in `ViewEditSubpage` (translated to all locales), and gated persistence in `JournalViewLeaf.getState` — `refDate` is written to the serialized layout only when the view opts in, so with the default a fresh launch centers on today. In-session navigation is unaffected (it reads the live reactive state, not the snapshot). Tests watched red first (getState omits `refDate` when the view does not remember; retains it when it does). E2e (`remember-date.e2e.ts`) drives the real view + toolbar navigation and asserts `app.workspace.getLayout()` (what a restart serializes/restores) carries no `refDate` by default, and carries the navigated month's date once the setting is toggled on through the real view editor.
  - v2's `refDateMoment = ref(today())` was component-local, reset every mount (`CalendarView.vue:21`). v3 round-tripped `refDate` through the persisted leaf state unconditionally, so a calendar paged to a distant month reopened there after restart. Now opt-in per view.
- [x] **83. Home block shows an empty-state placeholder where v2 rendered nothing.** **Decided won't-do (2026-07-16): the empty-state message is an intentional UX improvement.** A blank block reads as broken; "No journals to show. Check the block's show and shelf options." tells the user why nothing rendered and points at the fix. v2 rendered no children when `listToShow` was empty (`_old-code/code-blocks/home/HomeCodeBlock.vue:62-69`); v3 renders `m.code_blocks_home_empty()` (`src/code-blocks/home/ui/HomeCodeBlock.vue:76`).
- [x] **84. Command ribbon-icon tooltip now carries the journal/shelf prefix.** **Decided won't-do (2026-07-16): the prefixed tooltip is intentional.** It's consistent with the #46 palette prefix and disambiguates multiple same-named journal commands pinned to the ribbon (e.g. two journals' "Open today's note"). v2 passed the bare `command.name` as the ribbon tooltip (`utils/plugin-commands.ts:67`); v3 uses the prefixed palette name (`command-service.ts:70-82` via `command-registry.ts` `#paletteName`).
- [x] **85. Decoration date-condition description prints the literal string "null" for "any year".** [verified] Fixed: `describe-condition.ts` now renders a null year via `m.decoration_condition_date_any()` ("any"), matching the sibling day/month wildcard fields. Tests watched red first (null-year → "any", not "null"). v3-only UI defect (v2 rendered no per-condition descriptions); previously `year: c.year === null ? "null" : String(c.year)`, so the wildcard year renders the word "null" in the decorations settings summary, unlike the sibling day/month fields which use `m.decoration_condition_date_any()`.
- [x] **86. Minor deltas (#71-class).** [verified] (a), (b), (d) fixed and (c) confirmed intentional 2026-07-16. **(a) [fixed]** Custom journal's default nav first-row `fontSize` restored 2 → 3 (v2 parity); the config-default guard test was flipped red first. **(b) [fixed]** Frontmatter end-date write now suppresses an end equal to the **default period end** (not the start date — the audit's original note was wrong): `writeMutator` compares `metadata.endDate` to `CycleService.defaultEndOf` and persists it only when it is a genuine manual extension or `addEndDate` is on, matching v2's `toggleFrontmatterEndDate` guard (`journal.ts:473-474`, `resolveEndDate === end_date → delete`). Tests watched red first (suppress a default-equal end when addEndDate is off; still write it when on; a genuine extension is always kept). **(c) [intentional]** Home custom-entry label: v3 slices the label at the last `/` (showing the final path segment) and omits out-of-bounds entries (`home-items.ts:53-71`). The out-of-bounds omission is a strict improvement over v2's blank dead link. The slash-slicing is a deliberate, flagged choice: `NoteCreationSection` already warns when the date format or name template contains a path separator ("Date format contains a path separator. Move the date variables into the Folder field?", with an Apply link), so a slash that reaches the home label is user-informed. No change. **(d) [fixed]** "Add journal" name field regained a placeholder (`journal_add_modal_name_placeholder` = "e.g. Daily", translated to all locales).

## Checked and confirmed at parity (no finding)

Bounding the search — sweeps verified these as faithful (or already-tracked/intentional): all decoration evaluators + style derivation (v3 even fixes a latent v2 uniform-border padding bug) + per-write-type condition availability; period-badge/cell click·middle·context·hover·decoration·active wiring; active-note follow (week/quarter/year spillover); shelf-selector "All journals" scope; custom-intervals window/hideEmpty/active-highlight/offset-split; all five fence keys + nav link scoping + two-level context menu + timeline mode/weeks fallbacks; every v2 event (`file-open`, vault rename/delete, metadata changed) + core commands + URI (v3-new); migration maps every enumerated v2 field (only #12/#14/#15/#16 + `pendingMigrations` intentionally dropped); template selection (skip empty/nonexistent), reuse-existing-leaf on open, settings save/sync guards. #5 already records the delete-notes trash-vs-permanent delta; #52 already records the home-block at-cursor→suggest disambiguation change.

---

# Sixth-pass audit — 2026-07-16

Seven parallel domain sweeps (note lifecycle; config/cycle/numbering/index;
calendar+decorations; views/toolbar/blocks; code blocks; settings/config UI;
commands/host/migration), run **fresh** — the agents were told not to read this
doc, so every item below is an independent re-derivation from `_old-code/` vs
`src/`, then reconciled against #1–#86 by the coordinator. Items already tracked
were dropped. Items marked **[verified]** were re-read against v3 source by the
coordinator in this pass; the rest cite the sweep and still need an independent
read. All items are `[ ]` (open — decisions pending); several are v3 arguably
behaving _more correctly_ than v2 and may be kept.

## 🔴 Functional regressions (user-facing features lost)

- [x] **87. v2 weekly-journal notes silently disconnect on upgrade — no migration bridges the anchor change.** Fixed. Two paths: (1) `DataMigrationService` now writes the canonical `anchor` (already computed) into the date field instead of the raw start date — fixing v1-origin week/month/quarter/year notes reshaped via markers, which were all landing non-canonical (`data-migration-service.ts:97`). (2) The `v3ToV4` migration emits a new `{ kind: "week-anchor", journalName }` marker per weekly journal, and `DataMigrationService` consumes it to re-canonicalize the `journal-date` of already-v3-shaped weekly notes (week start → representative day). Unit-tested at the migration (`v3-to-v4.test.ts`), consumer (`data-migration-service.test.ts`: canonicalize / already-canonical no-op / other-journal ignored), and the `legacy-upgrade.e2e.ts` migration suite now asserts canonical anchors for week/month/quarter/year/interval notes (12/12 green against real Obsidian). The calendar week config is applied eagerly (`CalendarSettingsBridge`, `.eager()` + immediate `watchEffect`) before the readiness-gated walk, so `anchorOf` resolves with the migrated dow/doy. [verified]
  - Original finding: Week is the only write type where the canonical anchor ≠ the stored date: v2 stamped `journal-date` = week **start** (Monday under ISO — `_old-code/journals/fixed-interval.ts:107-117`), v3's canonical anchor is the representative day (Thursday — `src/calendar/period-week.ts:22-26`), and v3 `parseEntry` rejects any non-canonical `journal-date` (#53, `src/journals/frontmatter.ts:40-43`). `DataMigrationService` (`src/settings/legacy/data-migration-service.ts:70-108`) only rewrites notes whose `journal` key matches a _pending v1→v2 conversion marker_ (older community-plugin notes) — it never walks this plugin's own v2 notes, and `v3-to-v4` rewrites settings only. Result: after a real v2→v3 upgrade every existing weekly note fails to index/connect (calendar, nav, open-next/prev don't see it) and stays orphaned. Day/month/quarter/year are unaffected (anchor = start there). Belongs to the separately-tracked data-migration work, but that work must include a **weekly-note frontmatter rewrite** (Monday→representative-day) or these notes are lost. (Additionally: even the marker-driven path at `data-migration-service.ts:88,97` writes the raw `journal-start-date` value into `dateField`, not the resolved canonical `anchor` — so migrated weekly community notes would land non-canonical too.)

## 🟡 Capability changed shape / behavior changed (confirm intent)

- [x] **88. Unresolved `{{index}}` (and any conditionally-absent template var) renders as the literal token in filenames/paths; v2 emitted empty string.** Fixed: `NotePathService.contextFor` now binds every declared numbering source variable — the resolved number when present, else an empty string — so an unresolved `{{index}}` renders `""` (v2 parity) instead of leaking the literal token into the filename. Inversion is unaffected (it uses the separate `#parseContext`). Unit-tested in `note-path.test.ts` (custom journal, no resolved number → `sprints .md`), watched red first. [verified]
  - Original finding: `src/templates/engine.ts` `#renderVariable` returns `token.raw` for a variable missing from context; `src/journals/notes/note-path.ts` `contextFor` adds a numbering var only `if (value !== undefined)`. The default custom name template is `{{journal_name}} {{index}}` (`src/journals/config.ts:179`), so a custom journal whose numbering is disabled/unresolvable produces a filename literally containing `{{index}}` (e.g. `MyJournal {{index}}.md`) where v2 (`_old-code/utils/template.ts:63`, `(value ?? "").toString()`) produced `MyJournal `. Same latent risk for `{{start_date}}`/`{{end_date}}` when unresolvable. Fix: fall back to empty string for a missing string/number variable in filename/folder contexts (keep raw-token behavior only where round-trip inversion needs it).

- [x] **89. `open-next`/`open-prev` are gated on `activeNote().isSome()` (any active file), not the connection state.** Fixed (user chose connection-gating, an improvement over both v2 and the buggy v3): the commands now check `#activeEntry().isSome()` — available whenever the active note is connected to a journal, in **either editing or reading mode**, and hidden on a non-journal note, a non-markdown file, or no file. This is better than v2 (editor-only `editorCallback`, hidden in reading mode) and than the prior v3 (any active file, then a no-op "not connected" notice). The now-unreachable `command_open_not_connected` notice + message key were removed (all 11 locales). Note: `hasActiveEditor()` doesn't distinguish reading vs editing (a MarkdownView always has an editor), so editor-gating was the wrong axis anyway — the e2e proved it. Unit-tested (`navigation-commands.test.ts`) and e2e-validated (`commands.e2e.ts`: connected note in reading mode lists the commands; a non-journal note hides them). [verified]
  - Original finding: `src/journals/navigation-commands.ts:21,27` register `check: () => this.#workspace.activeNote().isSome()`, which is true in reading mode and on PDFs/images; v2 used `editorCallback` (`_old-code/main.ts:557,581`), and the #80 connect-note fix uses `hasActiveEditor()` (`note-connection-commands.ts:19`) with an explicit "v2 parity: editorCallback" comment. #27's closing note claimed open-next/prev already had editor gating; the code shows they don't. Align both to `hasActiveEditor()`.

- [x] **90. Timeline lower bound relaxed: a period straddling `timeline.start` is now in-bounds (v2 excluded it by anchor).** [verified] v2 `#checkBounds` rejected an anchor strictly before `start` (`_old-code/journals/journal.ts:668-672`); v3 `TimelineService.contains` gates on the period **end** (`src/journals/timeline.ts:18-22`), so e.g. a month whose 1st precedes a mid-month start is now in-bounds. Surfaces one extra partial period at the timeline start across `findAll`/auto-create/connect-bounds. Upper bounds (`repeats`, `date`) are at parity. Corroborated by two independent sweeps; looks deliberate (comment) — confirm and record, or restore anchor-based lower bound.

- [x] **91. Nav fence body is validated with a bare `v.object({})` (no `asRecord` wrapper) → a non-mapping fence now errors into a panel where v2 ignored it.** Fixed: extracted the duplicated `asRecord` into a shared `src/code-blocks/fence-record.ts` and fronted the nav schema with it (`navBlockSchema` in a new `nav-config.ts`), matching the home/timeline pattern; a scalar/sequence nav body degrades to `{}` and renders. Schema test watched red first. `src/code-blocks/nav/nav-block.ts:9` lacks the `v.pipe(v.unknown(), v.transform(asRecord), …)` front that home/timeline got in #67 (`home-config.ts`, `timeline-config.ts`); v2's nav processor never read the fence body at all. A `journal-nav` whose body parses to a scalar/sequence blanks into `code-block-error`. Clear internal inconsistency vs the sibling schemas — add the same `asRecord` guard.

- [x] **92. Unparseable YAML now blanks home/timeline into an error panel; v2 degraded.** **Decided won't-do (2026-07-16): keep the error panel.** The common typo case (`mode:month` with no space → a parseable scalar) already degrades via the #67/#91 `asRecord` tolerance; what remains is genuinely-malformed YAML (unbalanced brackets, etc.), exactly the case #50 deliberately chose an error panel for (clearer feedback than silently rendering defaults). No code change. `src/infrastructure/host/code-blocks/internal/code-block-service.ts:44-49` renders the error panel on any `parseYaml` throw, for every block. v2 home wrapped parse in try/catch → defaults (`_old-code/code-blocks/home/home-processor.ts:32-55`); v2 timeline never called `parseYaml` (line-split for `mode`), so neither ever errored on malformed YAML. Extends #50/#67, which only softened structurally-valid-but-wrong YAML, not parse failures. Decide whether parse-failure degradation is worth restoring for home/timeline.

- [x] **93. Home block lost the "current-journal" click narrowing.** v2 `HomeCodeBlock.vue:52-59`: clicking a non-custom entry whose write-type matches the current note's journal opened **only that journal**; v3 `src/code-blocks/home/ui/HomeCodeBlock.vue:65-71` always passes all matching journal names, so the same click now shows a multi-journal picker. Interaction regression when ≥2 same-type journals share a shelf.

- [x] **94. Property `eq`/`neq` narrowed from loose `==` to typed comparison.** `_old-code/composables/use-decorations.ts:181-185` used loose `==`/`!=`, so a numeric frontmatter `count: 5` matched `eq "5"`; v3 `src/decorations/engine-checks.ts:61-77` requires `typeof raw === "string"` for a `text` valueType, so an **untyped** numeric/boolean value no longer matches. Mitigated where Obsidian's metadata-type registry types the property (v3 auto-derives number/date/checkbox), but untyped-numeric configs regress. [medium]

- [x] **95. Numbering: a before-anchor date with a numbered neighbor no longer propagates.** v2 `#resolveIndex` derived from the nearest existing note _before_ applying the before-anchor cutoff, and cut off only for `increment` (`_old-code/journals/journal.ts:686-720`); v3 `NumberingService.#compute` returns `none` for `anchor < anchorDate` when `!allowBefore` _before_ consulting any neighbor and for all reset kinds (`src/journals/numbering.ts:48-49`). May sit inside the #19 decision — confirm whether the neighbor-propagation loss is intended. [medium]

- [x] **96. Custom-interval `countRepeats`/`anchorAtOffset` step by pure duration, ignoring the stored manual-extension chain.** v2 `custom-interval.ts:122-139` stepped via `resolveNext`, consulting the index + stored `end_date`; v3 `src/journals/cycle.ts:220-273` uses pure `customStepForward/Backward` (unlike `anchorOf`/`nextAnchor`, which do consult stored ends). For a custom journal with a manually extended/shrunk interval, computed (non-stored) index numbers diverge and `countRepeats` can undercount. [medium]

## 🟢 Behavioral / cosmetic deltas

- [x] **97. Connect-override trashes the occupant in the exact-path-collision subcase.** v2 `journal.ts:364-367` disconnected the occupant (kept the file, stripped frontmatter); v3 `src/journals/notes/note-connection.ts:112-116` trashes the occupant's file when the incoming note relocates onto its exact path (disconnects otherwise). Data-touching (recoverable via trash) — confirm intent.
- [x] **98. Same-anchor collision resolution flipped last-write-wins → incumbent-wins.** v2 index `set` overwrote the anchor slot (`_old-code/journals/journals-index.ts:116`); v3 keeps the incumbent and tracks the newcomer by path + logs a collision (`src/journals/journals-index.ts:44-77`). Sync-conflict copies now resolve to a different note. v3 is more defensive. [medium]
- [x] **99. today-vs-active highlight priority flipped.** Fixed (restore v2, 2026-07-16): reordered `NotesCalendarCell.vue` so `[data-active]` follows `[data-today]` — a cell that is both the open note and today resolves to the active colors again. v2 CSS put `[data-selected]` after `[data-today]` (active/selected won when a cell is both — `_old-code/components/calendar/CalendarGrid.vue:54-61`); v3 orders `[data-active]` before `[data-today]` so today wins (`src/notes-calendar/ui/NotesCalendarCell.vue:57-64`, deliberate per comment). Opposite of v2 for the open note on today's cell.
- [x] **100. `data-selected` → `data-active` attribute rename + outside-date color var `--code-comment` → `--text-muted`.** (`NotesCalendarCell.vue:33,57`, `NotesMonthView.vue:207-209`.) Breaks any user CSS snippet keyed on `[data-selected]`; slightly different muted shade. Release-note if kept.
- [x] **101. Appearance highlight CSS vars can go stale in a popout window.** Fixed (restore v2's per-instance approach, 2026-07-16): removed the document-body `CalendarAppearanceBridge` and bind the `--journal-cell-*` vars via a `useCalendarAppearanceStyle()` composable (`:style`) on every calendar surface — month/week views (covers cells in views and the timeline code block), period badges, and the custom-intervals block. Each renders in its own document, so any window (main or a later popout) reflects current colors and edits update live. Settings are resolved optionally so isolated component tests fall back to theme defaults. Pure `appearanceVariables()` mapping unit-tested. Previously a single `watchEffect` wrote `--journal-cell-*` onto `activeDocument.body` (`src/notes-calendar/appearance/bridge.ts:27-37`); a popout opened _after_ the last appearance edit shows default colors until the next change (conceded in the code comment). v2 `v-bind`-scoped them per calendar instance. [low]
- [x] **102. Home `separator: ""` no longer coerces to the bullet default.** Fixed (restore v2, 2026-07-16): the `separator` schema now folds an empty string to `" • "` via a transform (mirroring `scale`'s `|| 1`). Schema test watched red first. v2 `config.separator || " • "` forced `" • "` (`home-processor.ts:44`); v3 `v.fallback(v.string(), " • ")` passes `""` through (`src/code-blocks/home/home-config.ts:38`), rendering no separator. Test covers `null`→default but not `""`.
- [x] **103. Ribbon now requires a non-empty icon.** v2 added a ribbon for any `showInRibbon` command regardless of icon (`_old-code/utils/plugin-commands.ts:66-68`); v3 gates on `registration.ribbon && registration.icon` (`command-service.ts:70`), so a ribbon-flagged command with an empty icon gets no button. Arguably a bugfix.
- [x] **104. Migration drops the persisted `ui.calendarShelf`.** Fixed (2026-07-16): `reshapeViews` now takes the calendar shelf (`old.ui?.calendarShelf ?? null`) and writes it to the migrated view's `defaultShelf`, so a v2 user's calendar-shelf scoping survives the upgrade. Unit-tested (maps the shelf; null when unset). Previously `src/settings/legacy/v3-to-v4.ts` never mapped `old.ui.calendarShelf` into the migrated calendar view's `defaultShelf` (built as `null`), so a v2 user who scoped their calendar to a shelf gets an unscoped calendar after upgrade. Coupled to the intentional #12/#14 shelf redesign, but a `defaultShelf` slot exists to receive it — consider mapping it.
- [x] **105. Date-field _key_ rename → silent full disconnect (sharpens #57).** Fixed (reverses #57), and — per user decision (2026-07-16) — extended to **all four** renamable frontmatter keys, matching v2 which rewrote every field. `NoteConnectionService.renameFieldAll(journalName, oldKey, newKey)` moves the value across every connected note (snapshotting `entriesFor` before the config update, like `reconnectAll`); `EditFrontmatterFieldFlow` calls it for date/start/end fields and `EditSequencePropertyFlow` for the numbering key (both guarded on an actual change). The date field would otherwise orphan every note (`parseEntry` reads only `journal` + `dateField`; start/end/index are optional, so those renames only stranded a dead key or a manual index value). The now-false `journal_notes_not_rewritten_hint` was removed from both modals and dropped from all locales. Unit-tested at service + both flows + both modals (watched red first). Confirmed via `parseEntry`: the date field was the only rename that disconnected notes.
- [x] **106. Settings "repeats" number lost its "times" unit suffix.** Fixed: added `journal_edit_end_repeats_suffix` ("times", translated to all locales) and rendered it after the end-repeats `UiNumberInput` in `TimelineSection.vue`, mirroring the sequence reset-count suffix.
- [x] **107. Rename-journal modal dropped v2's "requires restart if you have ribbon icons and/or commands" warning** (`_old-code/components/modals/RenameJournal.modal.vue:53`). Likely fine if v3 re-registers commands/ribbon live on rename (needs a quick trace); flagged for confirmation. No `ReloadHintService.request()` on the rename path.
- [x] **108. Hover-preview modifier no longer platform-exclusive.** v2 watched Meta-on-macOS / Control-elsewhere (`_old-code/composables/use-hover-preview.ts:10`); v3 fires on Control **or** Meta on every platform (`src/ui/use-modifier-hover-preview.ts:18-32`). Widening, not a loss.
- [x] **109. Fresh-install default command names reworded** ("Open next week note" → "Open next weekly note", "last" → "previous"; `src/commands/config.ts:81-89`). Fresh installs only (migrated keep v2 names); matches the copy style guide. Cosmetic.
- [x] **110. Timeline "repeats" end-date _display_ differs by one period boundary** in settings (v2 showed the date just past the last period; v3 shows the last in-bounds day — `journal.ts:101-110` vs `src/journals/timeline.ts:44-54`). Actual in/out bound is identical; display-only. [medium]
- [x] **111. Connect modal dropped the note-path echo header** (`_old-code/components/modals/ConnectNote.modal.vue:143`). Fixed (restore v2, 2026-07-16): `ConnectNoteModal` shows the note path above the connect form again. Component test watched red first.

## Bucket B/C dispositions — ratified as intentional (2026-07-16)

The following were reviewed and **kept as v3 behaves** (v3 is equal-or-better; no code change):

- **#90** timeline lower bound includes a period straddling `timeline.start` — showing the overlapping period is reasonable.
- **#93** home-block click opens all matching journals (a picker when several) rather than jumping to the current note's journal — simpler, predictable (user chose keep-v3).
- **#94** property `eq`/`neq` typed comparison — more correct; the untyped-numeric case is rare and mitigated by Obsidian's type registry.
- **#95** numbering returns none for a before-anchor date when `!allowBefore`, before neighbor propagation — consistent with the #19 uniform-allowBefore decision.
- **#96** custom-interval `countRepeats`/`anchorAtOffset` step by pure duration — deliberate simplification; only diverges for a custom interval with a manually-edited `end_date`, and consulting stored ends here would risk the index cycle the v3 design avoids.
- **#97** connect-override trashes (recoverable) the occupant when relocating onto its exact path — resolves the collision v2 would hit.
- **#98** same-anchor collision keeps the incumbent + logs — more defensive than v2's last-write-wins.
- **#100** `data-selected` → `data-active` attribute + `--text-muted` outside-date var — cleaner; release-note for users with custom CSS keyed on `[data-selected]`.
- **#103** ribbon requires a non-empty icon — a blank ribbon button is useless.
- **#107** rename-journal restart warning dropped — verified obsolete: `DynamicCommandRegistry` re-registers commands (and their ribbon icons) on the journal `renamed` event (`command-registry.ts:255`), so no restart is needed.
- **#108** hover-preview fires on Control **or** Meta on all platforms — a widening, not a loss.
- **#109** fresh-install command names reworded ("weekly"/"previous") — matches the copy style guide; fresh installs only.
- **#110** timeline "repeats" end-date shows the last in-bounds day in settings — display-only; the actual bound is identical.

## Checked and confirmed at parity / already-tracked (sixth pass)

The seven fresh sweeps re-derived and dropped as already-tracked or intentional:
does-not-contain-on-absent (#76), border ridge/double (#33), bulk-add dry-run
default (#29), auto-create skips confirm (#54) + timing (#69), connect date
bounds (#23), ribbon tooltip prefix (#84), `reset_after` wrap range (#18),
`allowBefore` uniformity (#19), index canonical-anchor rejection (#53) + week
anchor (#20), `useShelves` (#14), dismissible notifications (#16),
rename-not-rewritten hint (#63/#57), `change-calendar-shelf` removal (#12/#14),
home empty-state (#83), home at-cursor→suggest (#52), fresh-install command
seeds (#21), custom `relative_date` labels/plural (#72). Independently
re-confirmed faithful: the full decoration condition×operator and style×option
matrices (v3 a typed-property superset); all cell/badge interactions
(click·middle·context·hover·active); leaf placement + `navigation=false` +
place-without-reveal startup (#74/#75); command ids/slugs + palette prefixes +
hotkey continuity (#46/#47); the 15 fresh-install seeds; every v2 event
subscription + guaranteed context-menu Delete (#45); template date modifiers +
decade math + template selection; numbering back/forward propagation (#77) and
month-end clamping; custom-locale week handling.

---

# Seventh-pass audit — 2026-07-17

Seven parallel sweeps (modals/flows; settings UI; calendar/views/toolbar;
copy+i18n; code blocks; commands/notifications/feedback; functional residue),
run **fresh** — agents were told not to read this doc, so every item is an
independent re-derivation, then reconciled against #1–#111 by the coordinator.
Already-tracked items were dropped (#84 ribbon tooltip, #93 home click
narrowing, #29 dry-run default, #14 always-on shelves, #16 banner channel).

**This pass was weighted toward v3 UX on its own merits, not v2 parity.** That
change of question is why it found what six parity passes could not: the v2-diff
question cannot generate a vault with zero journals, cannot see an interaction
between two separately-ratified decisions, and cannot fault v3 for a flaw v2
shared. Items marked **[verified]** were re-read against source by the
coordinator. All are `[ ]` (open — decisions pending).

## The four root causes

Most items below are symptoms of four causes; fix the cause, not the symptom.

**A. No error reaches the screen** (#112–#115, #126, #150–#151). `Flows.invoke`
logs and returns; ~40 of ~48 call sites `void` the result. This is also what
makes #116's data loss _invisible_.

**B. v3-new fence keys never got the #50 policy** (#131–#133). #50 ratified
"degrade, don't error" and implemented it for `mode`/`show`; #67 added the
`asRecord` front. `weeks`, `hiddenWeekdays`, and timeline `shelf` shipped later
and strict — they violate an established policy, they do not lack one.

**C. Referential integrity is unmaintained** (#122). Shelf rename/delete never
updates `View.defaultShelf`, though `shelves/service.ts:29` does exactly this
maintenance for journal renames.

**D. Fixed at the instance, not at the class** (#116, #119, #131–#132, #135–#136).
`59be58b1` fixed the anchor-string hole at 2 of 3 sites. The text audit marks
B12/B13 [fixed] at the instances found. #50 fixed the keys that existed then.

## 🔴 Data loss / functional regressions

- [x] **112. `Flows.invoke` never notifies — the silent-failure funnel.** [verified] `src/infrastructure/flows/flows.ts:32-42` routes every error to `logger`; the file has no `NoticeService` import. The returned `AsyncResult` is the only user channel and ~40 call sites discard it (`void flows.invoke(...)`). Only `journals/uri/journal-uri-handler.ts:64` notifies. **The same failure is reported over a URI and silent from the palette, ribbon, calendar click, and every code block.** A broken template path or unwritable folder on "Open today's note" produces: no note, no notice, nothing. Root cause A. Note `UserAborted`/`BenignFlowError` already carry the "stay silent" signal, so `invoke` could notify by default and opt out — but see #113 before centralizing on that flag. **Fixed (`c3aa6010`).** `invoke` now shows a notice for a genuine failure; `UserAborted`/`BenignFlowError` stay silent. Callers with better copy opt out via `{ notify: false }` (the URI handler, and `LoggingBlock` because `DumpLogsFlow` self-notifies) — both would otherwise double-notify. `Flows` now declares its `NoticeService` dependency, so 50 test containers register it; a lazy injector lookup would have avoided the churn but is a runtime service-locator lookup this codebase rejects.
- [x] **113. A command whose timeline has ended lists in the palette, runs, and silently no-ops.** [verified] `#listable`/`#plan` (`src/commands/command-registry.ts:97-109`) never consult `TimelineService.contains`; `OpenDateFlow` filters on exactly that (`open-date.flow.ts:49`) and returns `NoApplicableJournals`, which `implements BenignFlowError` (`journals/notes/errors.ts:10-13`) → logged at **info** → silent. The notice branch at `:199` is gated on `isAvailableType`, so it can never fire for `same`/`next`/`previous`. v2 shared one predicate between `checking` and execution (`_old-code/utils/plugin-commands.ts:38-43`): listed implied runnable. **The `benign` flag is miscarrying here** — this is an app bug, not a user cancel, and the flag actively downgrades it below the error log. Fix the classification before centralizing #112 on it. **Fixed (`d8eaf785`).** `#plan` filters candidates by `timeline.contains` at the resolved anchor and returns none when none survive, so listed ⇒ runnable (v2's property). `OpenDateFlow` keeps its filter as the backstop. Available types stay listed by their own predicate and now reach the "no previous/next note" notice instead of a silent flow error. `NoApplicableJournals` keeps its benign flag deliberately: it is now unreachable from a listed command, and its other callers either guard on the timeline (`useNotesCell.isActionable`) or handle it with better copy (the URI handler). Inverted the two e2e cases from `a66f3213`, which characterized the old behavior — decision confirmed 2026-07-17.
- [x] **114. Connect-note notifies on success only; failure is silent.** [verified] Both notices hang off `.tap` (`journals/notes/flows/connect-note.flow.ts:32,37`), which is ok-only per this repo's Result convention. No `.tapErr`. `ConnectError` includes `AnchorOccupiedError`/`NoteRenameError`/`NoteAlreadyExistsError` — all reachable from the modal's override/rename/move toggles. A rename collision closes the modal in silence, and because the user saw a success notice last time, **silence reads as success**. The asymmetry is worse than no notices at all. **Fixed by #112 (`c3aa6010`).** The funnel now notifies on failure, so the success-only asymmetry is gone. `connect-note.flow.test.ts` had asserted `notices.messages` was empty on failure — encoding this bug as intended; it now asserts the absence of the _success_ notice, and "a failure notifies" belongs to `flows.test.ts`.
- [ ] **115. Bulk-add: a bad folder closes the modal with no explanation.** `bulk-add.flow.ts:20` yields `FolderNotFoundError` (`bulk-add-service.ts:206-208`); discarded at `shelves/ui/JournalsDashboardBlock.vue:49` and `ShelfEditSubpage.vue:64`. Type a folder typo, click Next → modal closes, nothing opens, no message.
- [x] **116. 🔴 DATA LOSS: "End writing → Date" with no date picked wipes the entire journal config on reload.** [verified] **The third site of the `59be58b1` anchor-string hole.** `src/journals/config.ts:51` is `date: anchorString` while its two siblings got `optionalAnchorString` (`:53` `start`, `:80` `anchorDate`). `TimelineSection.vue:61` writes `{ kind: "date", date: "" as never }` — **the `as never` cast exists solely to silence the type system into writing a value the schema rejects** (and `as never` is already a documented smell in this repo). Confirmed by probe: `safeParse(timelineEnd, {kind:"date", date:""}).success === false`. `settings-service.ts:202-208` then replaces the **whole journal entry** with `defaultItem(id)` = a blank _daily_ journal. Repro: weekly journal → Timeline → End writing → Date → don't pick → restart. Journal returns as a bare daily journal: folder, name template, templates, nav rows, decorations all gone; every weekly note's `journal-date` is now non-canonical for a day cycle → `parseEntry` rejects all of them → **the journal's entire history vanishes from the calendar.** It logs a warning — to the log nobody reads (root cause A makes this invisible). Same hole on the migration path: `v3-to-v4.ts:17` maps `{type:"date", date}` straight through, so a v2 vault with `end:{type:"date"}` and no date resets at upgrade. Related: `v3-to-v4.ts:52` emits `{kind:"after", count}` while `config.ts:67` requires `minValue(1)`; a >12-month interval journal with `numeration_type:"year"` yields `resetAfter = 0` → same total reset, rarer input. **Fixed (`0934c09c`).** `end.date` takes `optionalAnchorString` like its two siblings; `contains` reads an unset end as no bound (comparing `anchor <= ""` would have bounded the journal to nothing, trading the data loss for #117); `endOf` returns None; `v3-to-v4` coerces a missing end date rather than emitting `undefined`. The `as never` cast is gone.
- [x] **117. "End writing → After N times" with no start date makes the journal write nothing.** v2 guarded the repeats bound on a valid start (`_old-code/journals/journal.ts:678-681`, `:105-106`); `src/journals/timeline.ts:26-29` has no guard. `cycle.ts:248-250` does `localMoment("", "YYYY-MM-DD", true)` → invalid → `diff` → `NaN` → `Math.ceil(NaN)` → `Option.some(NaN)` → `repeats.value < count` is `false` → `contains()` returns false for **every** anchor. Auto-create no-ops, bulk-add reports every note out-of-bounds, auto-attach never matches. `timeline.start` defaults to `EMPTY_ANCHOR` (`config.ts:334`), so a new journal is one dropdown away. The UI shows a "needs a start date" hint (`TimelineSection.vue:97-99`) but does not block it. v2 ignored the un-countable bound and kept working. No test covers empty-start (`timeline.test.ts:76-110`). **Fixed (`4a1d6c0f`).** A repeats bound with no start is un-countable, so `contains` leaves the journal unbounded (v2 required a start too) and `endOf` returns None instead of carrying an invalid moment through as a garbage date.
- [ ] **118. Auto-create runs at `onload`, before the index exists → duplicate notes.** v2 ran it inside `onLayoutReady` strictly after the vault walk (`_old-code/main.ts:384-388`). `src/main.ts:70` → `auto-create.ts:212-215` fires `void this.#tick()` immediately, **not** gated on layout-ready — `StartupOpenService` (`startup/startup-open.ts:53-58`) _is_ gated; auto-create is the odd one out. The index is built later (`vault-subscription.ts:135`), so the `indexed` lookup at `note-creation.ts:68` is guaranteed to miss. Repro: a bulk-added note kept its name (`Daily/Monday standup.md`, connected to today). Restart → `ensureNote` finds nothing at the derived `Daily/2026-07-17.md` → creates a **second** note for the same anchor → `JournalsIndex.register` logs an anchor collision and one note becomes a permanent orphan. Contradicts the repo's own documented `Vault-walk boot readiness` rule.
- [ ] **119. Dry run reports past-tense actions identical to a real run — and dry run is the default (#29).** [verified] `bulk-add-service.ts:153-194` pushes log lines **before** the `if (!dryRun)` guard in every branch: `"Merged into the note already connected to 2024-01-05; source deleted."`, `"Connected to Daily at 2024-01-05."`. `ProcessBulkAddModal.vue:146-152` renders them verbatim with no dry-run banner. v2 printed `Processed notes (dry run)` in the header (`BulkProcessNotes.vue:132`) — v3 dropped the header. **Compound with #29:** v2 could afford unlabeled past-tense logs because dry run was opt-in; #29 ratified dry-run-as-default (correctly — preview-first is safer), which made the misleading path the default one. Accept defaults, click Run, read 40 lines of "Connected to…", close the modal: nothing happened. Neither decision is wrong alone. Fix the tense/banner, keep #29.
- [ ] **120. Clearing "Date format" makes Continue silently do nothing.** [verified] Schema enforces `minLength(1)` (`bulk-add/config.ts:30`) but `ConfigureBulkAddModal.vue` renders **only** `errorBag.propertyName` (`:77`) — `errorBag.dateFormat` is never rendered. `handleSubmit` blocks, `api.submit` never fires, not one pixel changes. Textbook dead end; v2 rendered `<FormErrors :errors="errorBag.date_format" />` (`ConfigureBulkAddNotes.vue:141`). Violates the repo's own "field errors go in #description slot" rule.
- [ ] **122. Renaming or deleting a shelf silently orphans every view scoped to it.** [verified] The shelves `"renamed"` event has exactly **one** subscriber (`commands/command-registry.ts:257`, for commands). Nothing updates `View.defaultShelf`; `DeleteShelfFlow` reassigns _journals_ to a destination but never touches views. The only `defaultShelf` writers are the migration, create/update, and the settings dropdown. Result: `useShelfScope` returns `[]` → calendar empties; `ShelfSelectorItem.vue:20` advertises the **dead shelf name**, absent from its own dropdown; `ViewEditSubpage.vue:116-121` binds a `<select>` whose options are live shelves only, so a stale `defaultShelf` matches nothing and renders blank while the config still points at the ghost. **Caused by #104's fix:** before it, `defaultShelf` was always `null` for migrated users and this was unreachable. #104 is correct and should stay — it populated a field whose lifecycle nobody maintains, and migrated v2 users who scoped their calendar are now the _only_ population exposed. Self-indicting asymmetry: `shelves/service.ts:29` does exactly this maintenance for journal renames. Root cause C.
- [~] **123. View-block and toolbar-item controls are hover-only — unreachable on mobile, invisible to keyboard.** [verified] `views/ui/BlockFrame.vue:61-68` and `ToolbarItemFrame.vue:81-99` use `opacity: 0` (+ `pointer-events: none`) revealed only by `:hover`. **Zero `:focus-within` rules exist anywhere in `src/`.** `manifest.json` has `"isDesktopOnly": false`. This is the _only_ path to configuring or deleting a view's blocks and toolbar items, so on a phone a view's composition is permanently uneditable; keyboard users focus buttons that stay invisible and unclickable. Compounds with #148: v3 moved _more_ config behind this editor than v2 ever had there, which is what raises this from annoyance to unreachable-feature. **Partially fixed (`50f5e6f0`).** Both frames reveal their tools on `:focus-within` (keyboard), the drag suppression gained a `:focus-within` arm so it cannot be slipped past, and `BlockFrame` reveals its inline tools under `@media (hover: none)`. **Still open for touch on the toolbar strip:** `ToolbarItemFrame`'s tools are an absolutely-positioned popover per item, so revealing every item's popover at once would stack them across the strip. That needs a tap-to-reveal interaction — a design decision, not a CSS default.
- [ ] **124. Calendar cells are keyboard-focusable with no focus ring, no hover, and no aria-label.** [verified] `notes-calendar/ui/NotesCalendarCell.vue:29-32` correctly adds `role="button"`/`tabindex="0"`/Enter/Space — then styles only `cursor`, `[data-today]`, `[data-active]`. `grep "focus-visible|:hover" src/notes-calendar/` returns **nothing**. A keyboard user tabs ~35 day cells with zero indication of position; a mouse user gets no hover affordance. **Regression:** v2 used real `<button>`s (`_old-code/components/calendar/CalendarButton.vue:6`) which inherit Obsidian's focus treatment; v3's `<span tabindex="0">` gives that up while adding the tab stops. The sibling date-picker already ships both rules (`calendar/ui/CalendarGrid.vue:44-50`) — copy them. Also: no `aria-label` (a screen reader announces "5, button" — no month, no year, no "has a note"), and `today` is signalled by **color alone** (`appearance/slice.ts:20-21`: transparent background, accent text) where the date-picker adds `font-weight: bold` (`CalendarGrid.vue:58-61`).
- [ ] **128. First run is a dead end: a calendar that says "No journals on this shelf" when no shelf exists.** A fresh install seeds the default calendar view with `openOnStartup: true` + `showInRibbon: true` (`views/default-view.ts:26-35`) and no journal exists. `MonthCalendarBlock.vue:50-52` shows `messages/en.json:70` "No journals on this shelf." — but `viewContext.shelf.value` is `null` on first run, so **the copy names a concept the user has never met, about a shelf that doesn't exist**. No action, no "Create a journal", no settings link. The rest of the view still renders: the divider block draws its rule, `CustomIntervalsBlock.vue:110` loops over nothing. First-run panel = toolbar + grey sentence about shelves + stray horizontal rule. Branch the message on `shelf === null` and give it an action.
- [ ] **130. YAML errors discard the line number, the source, and the user's ability to fix it.** `infrastructure/host/code-blocks/internal/code-block-service.ts:44-49,74-76` renders one line: "Failed to parse code block YAML" — no line/column, no token, no fence name (the _schema_ path names the key; the YAML path doesn't), and `#renderError` calls `element.replaceChildren()` first so **the raw source is destroyed**. The parser's message, which carries line/column, goes **only to the console**. For a multi-line home fence the user is told something is wrong and given nothing to act on — devtools is not a reasonable ask for an Obsidian end user. Both panel strings are hardcoded English while the rest of the feature is localized.
- [ ] **131. `shelf: 2024` renders in the home block and blanks the timeline.** [verified] `home-config.ts:56-59` coerces a non-string shelf **with a comment explaining exactly why** ("an unquoted `shelf: 2024` parsed as a number"); `timeline-config.ts:22` is bare `v.optional(v.string())` and hard-errors. A shelf named `2024` is ordinary and YAML parses it as a number. Same line, same value, two blocks on one page, opposite outcomes. Root cause B.
- [ ] **132. `weeks`/`hiddenWeekdays` are strict, violating the #50 degrade policy.** Probed empirically: `mode: moth` → renders (correct per #50); `weeks: lft` → ERROR PANEL; `hiddenWeekdays: [0,7]` → ERROR PANEL. These are v3-new keys added after #50/#67 established the policy, so they never got the transform their siblings have (`timeline-config.ts:21` vs `:23-24`). Two lines apart in one schema, one forgives and one punishes. Root cause B + D.
- [ ] **135. Raw enum leaks into the name-template warning — and its test cannot fail.** [verified] `NoteCreationSection.vue:74` passes the whole `invertibility` object; `reason` is the raw union `"function-token" | "unknown-variable" | "clock-variable"` (`use-invertibility-check.ts:12`). Renders: _"Template cannot be reverse-parsed (**unknown-variable**: "mystery")…"_ — untranslatable in all 10 locales. This is the B12 class the text audit marks **[fixed]** (fixed at `journal_flow_failure` and `nav_block_section_use_defaults`; missed here). **`NoteCreationSection.test.ts:99-102` asserts the rendered text equals `m.journal_edit_name_template_invertibility_warning({reason:"unknown-variable", ...})` — it calls the same message function with the same args it is testing, so the assertion is a tautology that passes for any message body.** The leak is covered by a test that structurally cannot catch it. Also jargon: "reverse-parsed", "Auto-attach". Fix: make `reason` a selector (match block) per locale. Root cause D.

## 🟡 Capability changed shape / behavior changed

- [ ] **121. Bulk-add's result log is hardcoded English, bypassing i18n.** `bulk-add-service.ts:156,160,171,174,181-184,191` — every user-visible line is a raw template literal. A German user configures bulk-add in German, then gets the **only record of what happened to their notes** in English.
- [ ] **125. Middle-click is dead in the calendar grid and nowhere else.** `NotesCalendarCell.vue:37-42` binds only `@click`; browsers fire `auxclick` for button 1. Every other affordance wires `@auxclick.middle.prevent` (`PeriodButtonsItem.vue:115`, `ButtonItem.vue:160`, `NavBlockRow.vue:118`, `HomeCodeBlock.vue:79`, …), and `define-open-mode.ts:4` explicitly maps `button === 1` → `"tab"`. Middle-clicking a day does nothing while middle-clicking the month badge two rows above opens a tab. The `defineOpenMode` middle branch is unreachable from the grid.
- [ ] **126. A view block with an unknown key or invalid config vanishes silently.** `views/view-leaf.ts:143-151` — both failure paths `return null` after a `logger.warn`. This is the path a config referencing a deleted journal, or a schema-tightened upgrade, takes: the calendar loses a chunk of itself with no in-panel notice and no pointer to the view editor. Given the documented `Collection minLength reset trap` (and #116), the invalid-config branch is not hypothetical. Root cause A.
- [ ] **133. Typo'd top-level fence keys are silently swallowed.** `home-config.ts:18`/`timeline-config.ts:18` are non-strict `v.object`. Probed: `sohw: [week]` → renders the _default_ home block showing only "Today"; `moed: month` → `{}`. The block looks like it's working, so there is no signal at all — silent **and** plausible, the worst failure mode. v2 also ignored them, but v2 had no schema; v3 has the key list in hand and discards it. Suggest a non-fatal "unrecognized key" line beside a rendered block (consistent with #50's degrade policy).
- [ ] **134. The home block's journal picker is a center-screen modal where nav rows use a menu at the cursor.** `HomeCodeBlock.vue:66-70` omits `pickAt` → `open-date.flow.ts:70-72` falls back to `suggests.open(journalPickerSuggest, ...)`; `NavBlockRow.vue:97` **does** pass `pickAt: event` → a compact menu at the pointer. Identical "which journal?" question, two weights of answer. **Compounds with the ratified #93:** #93 chose to always show the picker (correct — simpler and predictable), which makes this heavier picker appear on _every_ home click rather than rarely. #93 was decided without this fact. Passing `pickAt: event` is a one-line change that honors #93 and removes the focus-stealing modal.
- [ ] **136. Three "Add …" modals whose confirm button says "Save".** The B13 class the text audit marks [fixed] — fixed for journal/shelf/view, missed at `decorations/settings/ui/EditDecorationModal.vue:130`, `commands/ui/EditCommandModal.vue:206`, `code-blocks/nav/settings/ui/EditNavBlockRowModal.vue:185`. Each title switches Add↔Edit while the CTA is unconditionally `common_action_submit()` = "Save". Mirror `ShelfNameModal.vue:58`. Root cause D.
- [ ] **137. The decoration condition editor renders unlabeled inputs — and the labels exist, orphaned.** Nine `decoration_condition_*_label` keys have no call site, and the fields they were written for render bare: `ConditionProperty.vue:92-99` (name input, operator dropdown, value input — all unlabeled), `ConditionTag.vue:15-20`, `ConditionTitle.vue`, `ConditionOffset.vue:17`, `ConditionWeekday.vue`. Screen-reader users get nothing. The labels are already translated in all 10 locales — wire them in. (`ConditionDate.vue` is the counter-example done right; its 2 orphans are genuinely dead.)
- [ ] **141. `<startOf=isoWeek>` / `<endOf=minute>` and friends are silently ignored.** v2 forwarded any unit string to moment (`_old-code/utils/template.ts:49-52`); `templates/modifiers.ts:9` fixes `BOUNDARY_UNITS` to `{year,quarter,month,week,day,decade,hour}` and `:24-26` returns the value **unchanged** for anything else. The token still parses (`grammar.ts:42` accepts `[a-zA-Z]+`) so there's no raw-text fallback, and `TemplateEngine.validate` never emits the `"unknown-unit"` problem it declares (`types.ts:37`), so settings shows no warning. Repro A: `{{date<startOf=isoWeek>:YYYY-MM-DD}}` on Fri 2026-07-17 with Sunday-start locale → v2 `2026-07-13.md`, v3 `2026-07-17.md` — a different filename that also breaks filename→date inversion for auto-attach/bulk-add. Repro B: `{{time<startOf=minute>:HH:mm:ss}}` → v2 `14:32:00`, v3 `14:32:47`.
- [ ] **142. Template variable names became case-sensitive.** [medium] v2 built every date/time variable regex with `"gi"` (`_old-code/utils/template.ts:16-21,80,89`); `templates/grammar.ts:40` has no `i` flag and `engine.ts:45-46` returns `token.raw` for an unknown name. Repro: name template `{{Date:YYYY-MM-DD}}` → v2 `2026-07-17.md`; v3 creates a note literally named `{{Date:YYYY-MM-DD}}.md` (`normalizePath` doesn't strip braces), and **every date then resolves to that same path** — day 2's note collides with day 1's. Same for `{{TIME}}`, `{{Current_Date}}`, `{{Journal_Name}}`. Medium only on how often users capitalize; the divergence and outcome are certain.
- [ ] **143. "Delete notes" never says how many notes it will delete.** `journals/settings/ui/DeleteJournalModal.vue:20-28` — the most destructive action in the plugin, and the copy is "All notes connected to this journal are deleted from your vault." `JournalsIndex` knows the exact count (`note-connection.ts:140` iterates precisely that set). "12 notes will be deleted" vs "some notes" is the difference between an informed confirm and a coin flip. Also: `notes-service.ts:170` uses `fileManager.trashFile` (recoverable), but the copy says "deleted from your vault", never "moved to trash" — so the user can't know that at the moment of decision. (v2 had the same gap; not a regression.)
- [ ] **144. Connect-note is offered with zero journals and dead-ends.** [medium] `note-connection-commands.ts:14-21` gates only on `hasActiveEditor()`. Fresh install → `journalNames = []` → `canConnect` false → Connect permanently disabled beside an empty dropdown, no empty state, no "create a journal first". Part of the #128 first-run story.
- [ ] **145. Shelves are exposed with no explanation of what a shelf is.** v2's "Use shelves?" toggle carried the explainer _and_ its two concrete effects ("Calendar View can be limited to display just one shelf", "Navigation code block will show decorations from journals on current shelf only") — `_old-code/settings/JournalSettingsDashboard.vue:100-110`. v3 renders the section unconditionally (`shelves/ui/ShelvesDashboardBlock.vue:47-60`) and **no string anywhere explains what a shelf does**. Always-on shelves is ratified (#14) and stays; the lost explainer is separate and is a free win — port it into the block description.
- [ ] **147. Subpage navigation never resets or restores scroll.** `settings/ui/settings-ui-service.ts:50-59` mutates only the stack; `SettingsDashboard.vue:21-27` swaps by `v-if`. `grep scrollTo|scrollIntoView|scrollTop src/` → nothing. Scroll to Journals, click edit → the subpage mounts mid-scroll with its back link off-screen above; press back → the dashboard returns at scroll 0 and you re-find the journal. v2 had the same flaw (carried-over, not a regression), but the push/pop stack is exactly the right seam and it's a two-line fix.
- [ ] **148. "Today button" / "Pick date button" modes moved from the settings root to five levels deep.** v2: two self-describing dropdowns in the Calendar block (`_old-code/settings/JournalSettingsDashboard.vue:182-195`). v3: Settings → Views → open view → Blocks → hover the Toolbar strip → hover an item → edit → mode (`views/toolbar-items/button/ui/ButtonItemConfig.vue:61-65`; migration confirms at `v3-to-v4.ts:108-109`). This is composable-views working as designed, not a bug — but step 5 is gated behind #123's hover-only controls, so on mobile the setting is unreachable. Flagged for the compound, not the move.
- [ ] **150. v2's navigation diagnostics are gone; the command is hidden instead.** v2 said "This note is not connected to any journal." (`_old-code/main.ts:562,586`); v3 hides `open-next`/`open-prev` on an unconnected note (`navigation-commands.ts:27,33`). Hiding beats a no-op and #89 decided this deliberately — but the _diagnostic_ is lost: a user on a note they believe is connected finds nothing in the palette and gets no hint that the connection is what's broken. ("There is no next note after this one." **is** preserved, `:50`.) Decide, don't necessarily revert.
- [ ] **151. `openNote` failure in journal navigation is logged, not shown.** `navigation-commands.ts:53-55` — `.tapErr(error => logger.error(...))` while `#notices` sits injected at `:16`. The entry is in the index but the file is gone/unreadable → nothing on screen. The one place that reaches for `tapErr` and still routes to the log. Root cause A.

## 🟢 Behavioral / cosmetic

- [ ] **127. "View was deleted" is a hardcoded, unstyled English literal.** `views/view-leaf.ts:137` — `h("div", {class:"journal-view-deleted"}, "View was deleted")` beside 129 localized `view_*` keys. The class has no styling anywhere, and there's no "close this leaf" action.
- [ ] **129. The error panel is unstyled — the promised CSS never shipped.** `code-block-service.ts:82` sets `className = "code-block-error"`; `.code-block-error` appears **nowhere** in `styles.css` or any scoped block. `docs/superpowers/specs/2026-05-27-v3-code-blocks-foundation-design.md:146` promised "styles ship with the block." The panel renders as bare text plus a bare `<ul>` — no red, no border, visually indistinguishable from note prose.
- [ ] **138. Four orphaned toolbar labels.** `view_toolbar_button_default_label_this_week`/`_this_month`/`_this_quarter`/`_this_year` — no call sites; `button-config.ts:64-81` replaced them with `m.relative_date_this({period})`. Pure dead copy; deleting removes 4 × 11 = 44 catalog entries.
- [ ] **139. `journal_edit_anchor_description` is broken English copied verbatim from v2.** `SequenceSection.vue:82` → "This date is used to connect some number to it for further calculations." (from `_old-code/settings/JournalSettingsEdit.vue:386`). Not actionable. The add-modal handles the same concept well: "This date is used to count custom intervals from." Suggest: "Numbering starts from this date. The note for this date gets the start number, and later notes count up from it."
- [ ] **140. Three live keys missing from every locale.** `bulk_add_occupant`, `bulk_add_target`, `shelf_name_placeholder` — used (`ProcessBulkAddModal.vue:102-103`, `ShelfNameModal.vue:52`) but absent from all 10 locale files; added after the last `translate:i18n` run. Otherwise locale drift is clean: every locale is exactly 613/616 with zero extras — a uniform gap, i.e. pipeline lag, not rot.
- [ ] **146. The clear-start-date button's tooltip says "Close".** `journals/settings/ui/sections/TimelineSection.vue:85-90` uses `:tooltip="m.common_action_close()"` ("Close") on the trash-icon button that clears the timeline start. v2 said "Clear start date" (`_old-code/settings/JournalSettingsEdit.vue:353-358`). A trash icon reading "Close" next to a date picker reads as "dismiss the picker" — the opposite of what it does. Needs its own key.
- [ ] **149. Configure-bulk-add lost its Cancel button.** v2 `ConfigureBulkAddNotes.vue:194`; v3 `ConfigureBulkAddModal.vue:151-153` has Continue alone. Esc and X still work; it's the odd one out in an otherwise rigorously consistent set.
- [ ] **152. The log dump is reachable only from settings.** `logging/settings/ui/LoggingBlock.vue:31` is the sole `DumpLogsFlow` caller — no command. Given #112–#115 leave the log as the _only_ record of a failure, a palette command would make bug reports materially easier. (The dump flow itself is the best-notified code in the plugin — `dump-logs.flow.ts:26,33,34` covers empty/failed/succeeded.)

## Found while fixing the seventh pass — 2026-07-17

- [ ] **153. `countRepeats` returns `Some(NaN)` for an unparseable `from`, and numbering still reaches it.** `src/journals/cycle.ts:244-251` parses `from` with `localMoment(from, "YYYY-MM-DD", true)`; an empty or malformed anchor yields an invalid moment, `b.diff(a, period)` is `NaN`, and `Math.ceil(NaN)` is wrapped as `Option.some(NaN)` — a "successful" count that silently poisons every comparison. #117 fixed the timeline caller by guarding at the semantic level (a repeats bound needs a start), but `src/journals/numbering.ts:61` still passes `anchorDate` — an `optionalAnchorString`, so legitimately `""` — straight in. The `allowBefore` guard above it (`:49`) does not catch it either: `anchor < ""` is false, so it falls through. **Repro:** a journal with numbering enabled and no anchor date set → `#cascade(sources, undefined, NaN)` → a `NaN` index rendered into the note. Fix at the source: `countRepeats` should return `Option.none()` when either bound does not parse, since `NaN` is not a count. Deliberately not bundled into #117, which fixed a different (semantic) defect.
- [ ] **154. A test that asserts against the message function it is testing cannot fail.** `src/journals/settings/ui/sections/NoteCreationSection.test.ts:99-102` asserts the rendered text equals `m.journal_edit_name_template_invertibility_warning({ reason: "unknown-variable", offending: "mystery" })` — calling the same message function with the same arguments it is verifying. The assertion passes for any message body, including the raw-enum leak in #135, so the leak is _covered by a test that structurally cannot catch it_. Worth a grep for the pattern (`m.*()` on both sides of an assertion) rather than a one-site fix; a component test should assert the literal user-visible string.

## Where v3 is better than v2 (recorded, no action)

Bulk-add gained a real plan-preview with per-note dropdowns, counts, and typed
skip reasons before anything is touched (v2 interrogated the user mid-run with
no way to see scope or back out) — which is what makes #119 such a shame: the
preview is excellent, then the results log undoes the trust it built. Bulk-add
also has correct progress feedback (`onProgress` → `bulk_add_progress`) and
collects per-note results into one log rather than N notices. Destructive modals
are now visually marked (`cta warning` + the verb) and spell out consequences
("Notes are not affected."). Real inline validation on create paths (vee-validate

- valibot: empty, duplicate, unchanged, anchor-required). `isActionable` respects
  the timeline where v2 offered to create notes outside configured bounds. The
  toolbar wraps where v2's header squashed. Middle-click and modifier-aware open
  modes on nav rows/arrows/home links (v2 had none). Dead nav arrows are suppressed.
  Real home-block empty state. `formatToRegexp` fixes v2 bugs. Ribbon teardown is
  handled (v2 leaked). `ViewEditSubpage` has zero orphaned fields.

## Checked and confirmed at parity / already-tracked (seventh pass)

Dropped as already-decided: ribbon tooltip prefix (#84), home click narrowing
(#93), dry-run default (#29), always-on shelves (#14), banner channel (#16),
`mode`/`show` degradation (#50/#67), open-next/prev gating (#89). Verified clean:
fence keys at full parity; `open-calendar`/`change-calendar-shelf` ids preserved;
`insert-date-link`/`connect-note` correctly editor-gated; `supportedTypes` a
superset of v2's; the code-block reference modal survived and documents the new
keys; zero en-GB spellings, zero `(s)` plurals, zero hardcoded Notice/setName
literals, zero concatenated tooltips (the §A pass held); `{count}` without a match
block is 4 deliberate hits; signed `countRepeats`, custom-interval month-end
clamping, `NoteMetadata.title`, and `parseEntry` type-guards all faithful;
`EditConfigModal` cancel semantics sound (configs are immutable);
`ViewNameModal` needs no uniqueness check (views are id-keyed); focus-on-open
absent in both v2 and v3 (parity-neutral); nav-row keyboard reachability a shared
gap, not a regression.
